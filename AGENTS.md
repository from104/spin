# AGENTS.md

이 저장소에서 일하는 에이전트가 지켜야 할 규칙 가운데, 특정 파일 하나에 딸리지 않는 저장소
공통 규칙을 모은다. 코드 규율은 각 파일 머리말 주석이 정본이다.

## CHANGELOG.md 작성 규칙 (2026-09-03 기현 지시)

> *"일단 현재의 체인지로그는 초기부터 너무 길었다. 단문으로 핵심만. 명사형 어미, 개발자가
> 아닌 철저한 이용자 관점으로."*

`CHANGELOG.md` 는 앱 화면의 [버전 번호]를 누르면 뜨는 변경 내역 모달이 그대로 읽어서 보여준다
(`src/app/changelog.ts` 가 `## [version]` 절을 파싱한다 — 별도 데이터로 옮겨 적지 않는다).
이용자가 직접 읽는 문서이므로 항목을 쓸 때마다 다음을 지킨다.

- **한 항목 = 한 줄.** 이어지는 설명 줄도, 하위 글머리(`  - `)도 두지 않는다.
- **명사형 어미로 끝맺는다.** "~습니다/합니다/됩니다" 로 끝나는 문장을 쓰지 않는다. "지원"·
  "추가"·"개선"·"수정"·"변경"·"제거"·"이동" 같은 명사로 끝낸다.
  예: "다크모드 지원", "로그인 오류 수정", "규칙 해설 검색엔진 노출 지원".
- **철저히 이용자 관점.** 왜 그렇게 만들었는지, 내부 구현이 어땠는지, 누가 무슨 지시를
  했는지, 어느 테스트를 고쳤는지, 어느 계획 문서 몇 절인지는 적지 않는다. **무엇이 달라졌는지**
  만 남긴다. 이용자가 마주치는 화면·버튼 이름은 대괄호로 표기한다(`[규칙]`, `[설정]` 처럼 —
  기존 관례).
- **핵심만.** 세부 사례를 나열하지 않는다. 한 덩어리 변경(예: 화면 하나를 전면 재작성)은 한
  줄로 뭉친다 — 서로 다른 변경이라 뜻이 갈릴 때만 줄을 나눈다.
- **굵게(`**...**`)·강조 기호(★) 를 쓰지 않는다.** 문장이 이미 짧아 강조가 필요 없다.
- 형식은 그대로 유지한다: `## [x.y.z] YYYY-MM-DD`(미배포는 `## [Unreleased]`, 날짜 없음) →
  `### 소제목`([Keep a Changelog] 범주 — 추가됨·변경됨·제거됨·고쳐짐 등) → `- 한 줄` 항목.
  `src/app/changelog.ts` 가 이 형식을 정규식으로 파싱하므로 헤딩 한 글자도 어긋나면 안 된다.
  한 버전 안에서 같은 소제목을 두 번 쓰지 않는다 — 추가됨·변경됨·제거됨·고쳐짐이 여럿 필요하면
  그때그때 하나로 합친다(0.6.0 절에 추가됨이 두 번 나뉘어 있던 것을 이번에 합쳤다).
- 파일 맨 끝의 `<!-- Links -->` 참조 링크 각주는 항상 모든 버전 절 **바깥**에 둔다 — 파서가
  그 줄부터 잘라내고 파싱한다(`changelog.ts` 의 `footerAt` 처리). 새 각주를 추가할 이유가
  생기면 이 각주 블록 하나에 더한다.

새 버전을 기록할 때마다 이 규칙을 따른다. 옛 항목의 문체를 참고해 옮겨 적지 않는다 — 옛 것이
이 규칙을 어기고 있으면 그건 아직 못 고친 것이지 본받을 스타일이 아니다.

### 세 언어를 함께 간다 (2026-09-03 기현 지시)

`CHANGELOG.md`(한국어, 정본) 옆에 `CHANGELOG.en.md`·`CHANGELOG.ja.md` 가 있다. 셋은 같은
구조(버전 헤딩·소제목·항목 수)를 유지하는 **번역본**이지 별개 문서가 아니다 — 모달이
`useLocale()` 로 셋 중 하나를 골라 그대로 보여준다(`ChangelogModal.tsx`).

- **새 버전을 기록할 때 세 파일을 같은 커밋에서 함께 채운다.** 한국어만 쓰고 영어·일본어를
  나중으로 미루지 않는다 — 미루는 순간 다른 언어로 쓰는 사람 눈에는 "달라진 게 없다"로 보인다.
- 번역은 그 언어의 **명사형 어미 관례**를 따른다 — 한국어를 그대로 옮기지 않는다.
  - 영어: Keep a Changelog 의 표준 소제목(Added/Changed/Removed/Fixed 등)을 쓰고, 항목은
    "Added dark mode", "Fixed login error" 처럼 짧은 명사구/과거분사로 끝낸다.
  - 일본어: 소제목은 追加/変更/削除/修正, 항목은 "ダークモード対応", "ログインエラー修正"
    처럼 명사(또는 名詞+する 앞의 어간)로 끝맺는다. "〜しました/です" 로 끝나는 문장을 쓰지
    않는다.
- 화면·버튼 이름(대괄호 표기)은 **그 언어의 실제 UI 문자열**을 쓴다 — 한국어 이름을 그대로
  옮기지 않는다. 정본은 `src/i18n/en.ts`·`src/i18n/ja.ts` 다(예: `[규칙]` → 영어는 `[Rules]`,
  일본어는 `[ルール]` — 각각 그 화면의 실제 레일 라벨).
- 세 파일의 버전 헤딩(`## [x.y.z] YYYY-MM-DD`)과 각 절의 항목 **개수**는 서로 같아야 한다 —
  한쪽에만 있는 사실은 없다. 개수가 다르면 번역이 빠졌거나 한쪽에 정보가 새로 끼어든 것이다.

## 테스트 작성 규칙 (2026-09-04 기현 지시)

> *"정말 테스트 코드가 코드 원문의 45%까지 필요해?"* → 감사 결과 테스트 코드는 앱 코드와 1:1 이었고,
> 렌더·문자열 테스트 2.9만 줄의 상당수가 버그가 아니라 **의도한 변경**에만 반응하는 감지기였다.
> 같은 날 139 파일을 감사관·반박관·실행관 3단으로 걸러 케이스 460개(줄 4,400)를 지우거나 합쳤다.

새 테스트를 쓰기 전에 한 문장으로 답한다: **"이 테스트를 지우면 어떤 실기 버그가 새는가?"** 답이
없으면 쓰지 않는다. 초록은 지킨다는 뜻이 아니다.

쓰지 않는 것(변경 감지기 · 자기증명):
- 라벨 문자열·항목 개수·순서만 보고 동작(상태 변화, 이벤트 결과)은 안 보는 것.
- 화면 DOM 의 스냅샷·해시·outerHTML 비교. 내보내기 SVG·인쇄 **출력물**의 골든만 예외다 — 그건
  이용자가 받는 파일이다.
- 소스의 상수·표·i18n 문자열을 베껴 와 같은지 보는 것.
- 다른 파일이 이미 검사하는 시나리오의 재검사(전술판 화면에서 편집기 키보드·드래그를 한 번 더 도는
  식). 의심되면 `rg` 로 먼저 찾는다.
- React·라이브러리 자체 동작(리렌더 횟수, setState 베일아웃)과 인라인 스타일 값(px·색 토큰 문자열).

쓰는 것:
- 구체적 회귀 — 실기에서 본 버그, 스키마 마이그레이션, 저장·복원, 되돌리기, 주소↔화면 매핑, 게이트.
- 접근성 계약 — aria 역할·이름, 포커스 이동, 키보드 조작, 라이브리전, 강제색·대비. 이 앱에서 a11y 는
  핵심 기능이다.
- 물리·좌표·판정 결과를 수치로 보는 것. 레이아웃 모드 전환(좁은 창·세로 창)에서 기능이 사라지지
  않는다는 것.
- `docs/FALSIFICATION-BASELINE.md` §4 수호 단언은 건드리지 않는다.

같은 render 설정에서 단언만 조금 다른 케이스는 하나로 합친다. 새 단언은 돌연변이(구현을 일부러
망가뜨려 빨간불 확인)로 값을 증명한 뒤 남긴다.
