# Changelog

이 프로젝트에 대한 모든 주목할만한 변경 사항은 이 파일에 기록됩니다.

형식은 [Keep a Changelog (korean)]를 기반으로 하며 이 프로젝트는 [Semantic Versioning (korean)]을 따릅니다.

## [Unreleased]

### 추가됨

- **★ [규칙]을 영어로 읽을 수 있습니다** — 9개 카드의 본문, 18개조 부록, 재개 비교표, 카드 목록이
  모두 영어로 나옵니다. 번역이 아니라 **FIPFA 영어 원문에서 새로 쓴 것**입니다(우리 한국어 정리가
  이미 영어 원문의 요약본이라, 그걸 번역하면 영어→한국어→영어가 됩니다).
  조항 도해(코트·공·장비·거리)의 라벨과 설명, 보드 장면의 코트 위 쪽지, 그리고 스크린리더가 읽는
  그림 설명과 선수 호칭까지 **전부** 영어로 나옵니다. 일본어는 아직 준비 중이라 기존 안내가 뜹니다.
  일본어는 아직 준비 중이라 기존 안내가 그대로 뜹니다.

### 고쳐짐

- **페널티킥에서 "손을 쓴 경우는 예외" 라고 적혀 있던 것을 바로잡았습니다** — 예외(면제)가 아니라
  **더 무거운 제재**입니다. 킥을 찬 사람이 공을 두 번째로 만지면 상대에게 간접프리킥인데, 그
  두 번째 터치가 고의 핸드볼이면 **직접프리킥**입니다. 규칙 정리 문서와 [규칙] 화면 양쪽에
  오래 면제처럼 적혀 있었습니다.

- **[규칙]에 빠져 있던 조항들이 들어왔습니다** — 규칙 정리의 바탕이 되는 문서가 FIPFA 원문의
  일부를 빠뜨리고 있었습니다. 원문을 직접 확인해 채웠고, 그 결과 [규칙]이 이런 것들을 말할 수
  있게 됐습니다: **손·발·몸이 닿아 공이 움직이면 안 된다**(공은 체어로만 움직입니다) ·
  **선수 등급 PF1·PF2와 "한 팀이 PF2를 두 명 넘게 낼 수 없다"** · **태클은 프런트가드끼리일 때만
  허용**(램핑·홀딩·클리핑이 무엇인지도) · 아웃이 되는 나머지 조건(**5초 물림**, 50.8cm 위 위험).

- **한국어가 아닌 언어로 쓸 때 [규칙]이 고장 난 것처럼 보이던 문제** — 앱 UI 는 한·영·일이 다
  번역돼 있지만 [규칙]의 **내용은 아직 한국어뿐**입니다. 그래서 영어·일본어로 쓰면 버튼만
  번역되고 본문은 한국어인 화면이 나왔고, 왜 그런지 알 길이 없었습니다. 이제 [규칙] 맨 위에
  **읽는 분의 언어로 안내**가 뜨고, FIPFA가 펴낸 **영어 원문 PDF 주소**를 함께 드립니다.
  한국어로 쓰실 때는 아무것도 바뀌지 않습니다.

### 추가됨

- **[게임의 목적]에 「이 경기가 남기는 것」이 붙었습니다** — 중증 장애가 있는 사람이 이 종목에서
  무엇을 얻는지, 그리고 무엇이 아직 막혀 있는지를 마지막 묶음으로 답합니다. 동료심사 논문
  한 편(2025, 오픈액세스)에서만 뽑았고, 표본이 열 명인 작은 연구라는 사실을 문장 안에 밝혔습니다.
  선수들이 직접 꼽은 장벽에 **"규칙을 아는 심판과 코치가 모자란다"** 가 들어 있습니다.

- **★ [규칙]이 종목 소개부터 시작합니다 — 카드 두 장이 새로 앞에 섰습니다** — 그동안 [규칙]을
  처음 열면 첫 카드가 [기본 규칙](코트 28×15m)이라, 이 종목이 무엇인지 한 줄도 없이 코트
  규격부터 읽게 됐습니다. 이제 **[파워체어풋볼이란]**과 **[게임의 목적]**이 차례로 먼저 뜹니다.
  이 두 장이 뒤 일곱 장의 세부 규칙을 읽을 바탕입니다.

  소제목은 이제 본문보다 크고 아래에 가는 선이 깔려, 긴 카드에서도 원하는 묶음을 훑어 찾을 수
  있습니다(그전에는 본문과 글자 크기가 같아 굵기로만 구분됐습니다).

  두 장은 **소제목을 단 여러 묶음으로 늘어나** 처음 보는 사람이 궁금해할 것을 차례로 답합니다.
  [파워체어풋볼이란]은 이 종목이 무엇이고 어떻게 불리는지, 코트 위에 무엇이 있는지, **공은 왜
  그렇게 크고 골대에는 왜 그물이 없는지**, 누가 뛰는지(휠체어를 안전하게 다룰 수 있으면 되고
  남녀가 한 팀에서 같이 뜁니다), 규칙이 어떻게 한 벌이 됐는지, 그리고 **지금 세계와 한국은
  어떤지**(30개가 넘는 나라 · 국내 17개 클럽 200명 이상 · 아직 패럴림픽 정식 종목은 아님)까지
  이어집니다. [게임의 목적]은 40분과 하프타임·진영 교대, 무엇이 골이고 무엇이 골이 아닌지,
  공을 무엇으로 어떻게 움직이는지, **왜 규칙이 선수들을 흩어 놓는지**, 공이 멈추거나 반칙이
  나오면 무슨 일이 벌어지는지를 짧은 문장으로 짚어 줍니다. 문장은 길어지지 않았습니다 —
  짧은 문장이 더 많아졌을 뿐입니다.

- **골대에 받침판이 생겼습니다** — 실물 골대는 기둥만 서 있는 게 아니라 무게추 사각판 위에
  꽂혀 있고, 그 판은 코트 안으로 들어오지 않도록 **골라인 바깥 + 가까운 사이드라인 쪽**으로
  비켜 놓입니다. 화면·시연·PNG·인쇄 전부에 같은 모양으로 그려지고, 편집기에서 골대가 밀리면
  판도 함께 따라갑니다. 판은 그림일 뿐이라 부딪히지 않습니다 — 충돌은 예전처럼 기둥만 합니다.
  실물은 50 cm 인데 화면에는 **75 cm 로 그립니다** — 30 m 코트를 한 화면에 담으면 50 cm 판이
  기둥 테두리처럼 보여서 골대가 무엇 위에 서 있는지가 안 읽힙니다. 판 색은 **짙은 벽돌색**
  입니다 — 기둥 테두리와 같은 계열이라 한 장비로 읽히고, 콘의 주황과는 밝기가 크게 갈려
  코트 위에서 헷갈리지 않습니다.

- **미세 이동 패드 — 키보드가 없는 태블릿에서도 정밀하게 놓습니다** — 개체를 길게 눌러(오른쪽
  클릭) 나오는 메뉴 맨 위에 방향 4칸과 회전 2칸이 섰습니다. 걸음은 키보드의 무수식 기본값과
  **같은 값**(2.5px·5°)이고, 누르고 있으면 이어집니다. 그동안 터치에서 개체를 놓는 길은 드래그
  뿐이라 그보다 정밀하게 맞출 방법이 아예 없었습니다. 회전은 방향을 가진 휠체어 하나를 골랐을
  때만 뜨고, 여럿을 고르면 이동만 뜹니다. 이 칸들만은 눌러도 메뉴가 닫히지 않습니다.

- **밀린 골대를 직접 눌러 되돌립니다** — 제자리에서 벗어난 골대 위에 마우스를 올리면 커서가
  되돌리기 화살표로 바뀌고, 누르면 **모든** 골대가 제자리로 갑니다. [보드 설정] 안
  [골대 원위치]와 같은 동작이라 어느 쪽으로 눌러도 결과가 같습니다. 제자리에 있는 골대는
  커서가 바뀌지 않고 눌리지도 않습니다. 밀린 골대에는 **실선 강조 링**이, 원래 자리에는
  **점선 유령**이 함께 뜹니다 — 커서는 마우스에만 있어서, 터치에서는 누를 수 있다는 것을
  알 길이 없었습니다.

### 변경됨

- **★ [규칙] 카드가 여덟 장에서 아홉 장이 됐고, 글을 전부 다시 썼습니다** — 순서는
  [파워체어풋볼이란] · [게임의 목적] · [선수·코트·공·장비] · [인·아웃과 득점] · [경기 재개] ·
  [골에어리어 반칙] · [2-on-1 반칙] · [그 외의 반칙] · [공식 룰 북] 입니다. 앞의 두 장이
  새로 섰고(위 [추가됨]), **[경합: 누구 터치아웃?]은 없어졌습니다** — 산문 두 문장과 장면
  하나뿐이라 카드 홈에서 다른 카드와 같은 자리를 차지할 값이 안 됐고, 그 판정(터치라인 동시
  접촉은 바깥쪽 팀에게)은 원문에서도 킥인 조항 **안에** 있습니다. 내용은 한 줄도 잃지 않고
  [경기 재개] 카드의 마지막 두 블록으로 옮겼습니다(장면 포함). **옛 주소
  `/rules/contested` 를 북마크해 두셨다면 그대로 열리고 [경기 재개]로 이동합니다.**
  카드 제목도 내용에 맞게 손봤습니다: [기본 규칙] → [선수·코트·공·장비],
  [경기 재개 한눈에] → [경기 재개], [아웃 오브 플레이] → [인·아웃과 득점],
  [2-on-1] → [2-on-1 반칙].

- **[규칙]의 설명 글을 처음부터 다시 썼습니다** — 아홉 장 전부입니다. 한 문장에 조건 서넛이
  겹쳐 있던 문단을 나누고, 원문에 없는 앱 조어(*"회피 이탈"* 등)를 원문 표현으로 바꾸고,
  "2-on-1은 관여해야 반칙"이라고 해 놓고 정작 관여가 무엇인지 말하지 않던 구멍을 메웠습니다.
  [공식 룰 북] 카드에는 FIPFA 원문 PDF 주소가 들어갔습니다 — 그동안 앱 어디에도 원문으로
  가는 길이 한 건도 없으면서 "원문을 확인하세요"라고만 적혀 있었습니다.

- **휠체어 이동·회전 잡는 범위가 2:1 이 됐습니다** — 차체를 반씩 나눠 뒤 절반이 이동, 앞
  절반이 회전이었는데, 이제 **뒤 2/3 가 이동 · 앞 1/3 이 회전**입니다. 회전은 차체 밖 앞
  손잡이라는 넓은 두 번째 문이 있지만(아래 항목), 이동은 차체를 잡는 길 하나뿐입니다.

- **앞뒤 손잡이로 끌 때 회전이 먼저입니다** — 앞 손잡이를 **바로 뒤로** 끌면 이제 휠체어가
  **제자리에서 돌아** 손끝을 따라옵니다. 그동안은 손가락이 차체 쪽으로 들어오는 동안 줄이
  느슨하다고 보고 회전을 아예 껐기 때문에, 180° 로 끄는 손짓이 사실은 **후진**이었습니다.
  급하게 꺾어 끄는 손짓이 안 먹던 것도 같은 이유입니다. 손잡이를 안쪽으로 조금 당겨
  후진시키던 방법은 없어졌습니다 — 그 일은 넓어진 이동 구역(위 항목)이 맡습니다.

- **휠체어 치수를 실물 실측값으로 바꿨습니다** — 1.50 × 1.00 m 에서 **1.30 × 0.80 m** 으로
  (기현님 실측). 처음에 큰 쪽을 고른 이유는 물리가 아니라 글자였습니다 — 등번호가 좁은 칩에
  안 들어갔거든요. 그 글자는 2026-08-11 에 이미 2/3 로 줄어 이유가 사라져 있었습니다.
  화면의 칩, 트레이 그림, PNG 내보내기, 반칙 판정의 겹침 계산, 밀고 밀리는 물리가 전부 이
  치수에서 나오므로 함께 따라갑니다. 차체가 짧아져 **제자리 회전이 조금 빨라집니다**(같은
  범퍼 속도라도 지레가 짧으면 더 빨리 돕니다).

- **스텝 사이드바의 [한 장 더 찍기] 버튼을 없앴습니다** — 스텝을 늘리는 길은 이제 카드 사이와
  목록 끝의 [+] 하나입니다. 두 길이 하던 일이 똑같았거든요(위 스텝을 복제해 넣고 그 장으로
  이동). [+]는 **자리를 고를 수 있다는 점만 더** 하고, 없어진 버튼이 하던 "맨 뒤에 한 장"은
  목록 끝 [+]가 그대로 합니다.

- **[정보] 버튼이 오른쪽 기능바 맨 위로 올라갔습니다** — 드릴 편집(연필 아이콘)과 시연(눈
  아이콘) 두 화면 모두입니다. 두 화면을 오갈 때 같은 높이에서 찾게 됩니다. 그 아래 칸들은
  한 칸씩 내려갑니다.

- **드릴 편집·시연 화면의 제목이 가운데에 섭니다** — 드릴 이름, [편집중] 배지, 이름을 고치는
  입력 칸이 모두 가운데 정렬입니다. 다른 화면(목록·설정 등)은 부제·설명이 함께 서기 때문에
  왼쪽 정렬 그대로입니다.

### 고쳐짐

- **[규칙]이 잘못 말하던 사실들** — 전면 재작성(위 [변경됨])에서 함께 잡았습니다.
  ① *"정지된 공이 다시 살아나는 방법은 7가지"* — 원문 기준 재개는 **세트볼까지 여덟 가지**
  입니다. "7종"은 재개 전체의 수가 아니라 **투터치 금지가 명시된 재개의 수**였는데, 그 숫자가
  카드 제목과 첫 문장으로 올라가 있었습니다. 표는 그대로 일곱 열이고, 앞 카드에서 본 세트볼을
  더해 여덟이라고 문장이 밝힙니다. ② *"이 다섯이 간접프리킥 대상입니다"* — 원문은 **여섯**
  입니다. 빠져 있던 여섯째(Law 12에 없는 반칙으로 경고·퇴장 때문에 경기를 멈춘 경우)를
  살렸고, 목록을 개수로 닫지 않게 고쳤습니다. ③ [아웃 오브 플레이] 카드가 앞면에 **투터치**를
  광고했는데 **그 카드 안에는 투터치 이야기가 한 줄도 없었습니다** — 이제 카드 앞면은 그 카드가
  실제로 담은 것만 적습니다.

- **설정의 [제자리 회전 시작] 조절 막대가 기본값을 담지 못하던 문제** — 막대 범위가 0.22~0.45
  인데 기본값이 0.5(지금은 0.67)라, 엄지가 끝에 붙어 있고 **건드리는 순간 값이 조용히
  내려갔습니다**. 범위를 실제로 허용되는 끝(0.9)까지 넓혔습니다.

- **코트 형태·크기의 설명이 화면에 보입니다** — 풀·하프·플랫이 각각 무엇인지, 30×18·28×15·
  25×14 가 어떤 체육관에 맞는지는 **툴팁에만** 있었습니다. 툴팁은 터치에서 뜨지 않으므로,
  태블릿으로 여는 사람에게 그 여섯 선택지는 이름만 있고 뜻이 없는 버튼이었습니다 — 그리고
  이건 드릴을 만들 때 가장 먼저 내리는 결정입니다. 이제 [새 드릴]과 [보드 설정] 두 곳 모두
  이름 아래에 한 줄 설명이 섭니다. 진영이 무엇을 정하는지는 도움말에 적었습니다.

- **동기화가 당겨온 명단이 설정 화면에 안 나타나던 문제** — 다른 기기에서 고친 선수 명단을
  드라이브가 당겨 와도, 설정 화면이 열려 있으면 옛 명단을 계속 보여 줬습니다. 그 상태에서
  선수 하나만 고치면 **방금 당겨온 명단을 옛것으로 덮어썼습니다**(명단은 문서 통째로
  저장됩니다). 백업 복원 쪽에는 이미 방어가 있었는데 동기화 쪽만 비어 있었습니다.

- **[골대 원위치]가 눌러도 아무 일이 없던 문제** — 골대는 순간이동하지 않고 0.5초에 걸쳐
  밀고 들어가는데, 그 움직임을 굴리는 루프가 **첫 프레임에서 꺼졌습니다.** 버튼은 "돌아가라"고
  표시만 하고 실제로 미는 일은 다음 물리 스텝이 하는데, 그 스텝이 돌기 전에 루프가 "아무도
  안 움직이네" 하고 판단해 버린 것입니다. 화면 주사율이 높을수록(120Hz 등) 반드시 그렇게
  됐습니다. 이제 골대가 돌아오는 중이면 "판이 다 섰다"고 판단하지 않습니다.

- **드릴 목록이 편집 내용을 바로 반영합니다** — 드릴 제목을 고치고 목록으로 나가도 옛 제목·옛
  수정시각이 그대로 서 있었습니다. 목록은 앱을 켤 때 한 번 읽고, 그 뒤로는 **바꾼 쪽이 알려
  줘야** 갱신되는 구조였는데 만들기·복제·삭제·가져오기·동기화는 알려 주고 **편집기만 안 알려
  줬습니다**. 이제 목록이 저장소의 쓰기를 직접 듣습니다 — 어디서 무엇을 바꾸든 따라옵니다.
  다른 탭에서 바꾼 것도 함께 반영됩니다.

## [0.5.99] 2026-08-28

0.6 을 앞둔 사전 배포판입니다. 규칙 화면(주제별 학습) · 삭제 안전망 · 설정 정리에 더해,
전술판과 드릴 편집의 조작이 이번 회차에 크게 정리됐습니다 — 드릴을 만드는 두 길(이름·코트를
먼저 묻는 [새 드릴], 그리던 판에 이름을 붙이는 [저장])이 갈렸고, [비우기]와 코트 전환이
되돌리기에 들어왔고, 판 관련 조작이 [보드 설정] 하나로 모였습니다.


### 추가됨

- **★ 규칙 화면이 생겼습니다 — 조항 순서가 아니라 주제로 익힙니다** — 왼쪽 레일(세션과
  설정 사이)에 새로 생긴 [규칙]을 열면 조항 목록 대신 8개 주제 카드가 뜹니다: 기본 규칙
  (코트·선수·공·장비) · 경기 재개 한눈에(비교표) · 아웃 오브 플레이(라인·5m·세트볼·투터치)
  · 골에어리어 반칙 · 2-on-1 · 그 외의 반칙(램핑·회전킥·카드) · 경합(누구 터치아웃?) ·
  공식 룰 북(18개조 압축 참조). 카드를 고르면 그 주제의 설명 글·도해·**보드 애니메이션**을
  한 화면에서 순서대로 읽습니다 — 공 주위 3m/5m 원은 전술판·시연에서 보던 그 판정 링과
  같습니다. 조항 번호로 찾아보고 싶을 땐 [공식 룰 북] 카드에 18개조 압축 요약이 있습니다.
  각 장면은 재생 버튼을 누르기 전까지 정지 이미지(포스터)이고, 한 번에 하나만 재생됩니다.
  주소창에 지금 보는 주제가 그대로 남아 새로고침·공유·뒤로가기가 됩니다. 처음 열면 다른
  화면처럼 3단계 튜토리얼이 뜨고, [도움말]에서 언제든 다시 볼 수 있습니다.

- **경기 재개 7가지를 표 하나로 비교합니다** — [규칙] > [경기 재개 한눈에]에서 킥오프·
  킥인·코너킥·골킥·직접/간접 프리킥·페널티킥을 트리거·볼 위치·상대 거리·직접 득점 여부·
  특이사항 기준으로 한 표에 놓고 비교합니다. 재개 하나를 고르면 그 아래에서 실제 보드
  장면이 재생됩니다. 좁은 화면에서는 표 대신 재개별 카드로 접힙니다.

- **2-on-1 예외까지 장면으로 봅니다** — [규칙] > [2-on-1]에서 기본 위반 장면에 더해
  골키퍼 예외·상대가 아예 없는 경우·회피 이탈(합법적으로 잠시 필드를 벗어났다 돌아오는
  조건)까지 5개 장면으로 나눠 봅니다. [아웃 오브 플레이]에는 세트볼(참여 2명만 30cm,
  나머지 3m) 장면이, [그 외의 반칙]에는 회전킥(왜 위험할 수 있는지)이, [경합]에는 터치라인
  동시 접촉 판정 장면이 새로 생겼습니다.

- **★ 글자만 있던 조항에 그림이 붙습니다** — 판정이 없어 보드를 붙일 수 없는 조항은 요약
  몇 줄만 덩그러니 남았습니다. 코트 위의 사건이 아닌 조항(공의 규격·장비·경기 시간 같은
  것)에는 빈 판을 띄우는 대신 **한 장짜리 도해**를 놓기로 했습니다. 첫 대상은 **제2조(공)**
  입니다: 경기구를 축구공 5호와 **같은 축척으로 나란히** 세워 지름 33cm 가 얼마나 큰지
  눈으로 재게 하고, 규칙이 공에 대해 정하는 유일한 조건인 **공기압**을 낮음·알맞음·높음
  세 칸으로 그렸습니다 — 물렁하면 체어가 타고 넘고, 팽팽하면 튀어 오르고, 알맞으면
  볼가드에 걸려 굴러 나갑니다. 조문도 다시 썼습니다(파열 시 재개 방법 두 가지, 주심 승인
  없는 교체 금지가 빠져 있었습니다).

- **★ 제4조(선수 장비)에도 도해가 붙습니다** — 경기용 파워체어 옆모습에 프런트가드·
  시트벨트·측면지지대·전도방지 바·후방 캐스터·밑판 경계선을 라벨로 표시하고, 왜 그 자리에
  있는지 캡션으로 풀었습니다. 전도방지 바는 제2조가 요구하는 "체어가 공을 타고 넘지 못하게"
  를 체어 쪽에서 구현한 부착물입니다. 두 번째 그림은 "전진·후진 공통 10km/h" 규정을 일반
  전동휠체어·스쿠터(후진이 더 느린 경우가 많음)와 대조해 보여줍니다. 실물 근거는
  스트라이크포스(Power Soccer Shop) 사용설명서이고, 체어 그림은 제2조 도해와 같은 것을
  재사용합니다.

- **★ 제1조(필드)에도 도해 두 장이 붙습니다** — 코트 규격 3단(최대 30×18m·표준 28×15m·
  최소 25×14m)을 같은 축척으로 겹쳐 그려, 표준 규격이 정확히 농구 코트 크기라는 사실을
  한눈에 보여줍니다. 두 번째 그림은 바닥재 권장·지양(목재·인조 마루 vs 콘크리트·아스팔트)
  을 바퀴 접지 모양으로 비교합니다. 기존 보드 장면(정지 도해)이 이미 보여주던 골에어리어·
  페널티 마크 같은 도형은 겹치지 않고, 장면이 안 다루던 것만 새로 그렸습니다.

### 변경됨

### 고쳐짐

- **키 이름을 안 실어 보내는 입력 장치에서 문자 단축키가 죽던 비대칭** — 앱은 자판 배열·입력기와
  무관하도록 **물리 키 이름**(`KeyD`)으로 단축키를 잡습니다. 화면 키보드·매크로 같은 일부 장치는
  그 이름을 안 보내는데, 그때 방향키는 이름이 같아서(`ArrowRight`) 살아남고 문자키만 `d` ≠ `KeyD`
  로 조용히 죽었습니다. 이제 그런 사건에서 **ASCII 한 글자를 물리 키 이름으로 되살립니다**
  (`[` `]` `/` `=` `-` `0` 도 함께). 한글 오발화 방어는 그대로입니다 — 되살리는 대상이 ASCII
  한 글자뿐이라 `ㅈ`·`ㅍ` 는 여전히 아무 데도 안 맞습니다.

- **★ 키보드 이동·회전이 기본은 미세, Shift 가 큰 걸음으로 뒤집혔습니다** — W A S D·방향키는
  이제 **2.5px**, Q E 회전은 **5°** 씩 움직이고, `Shift` 를 함께 누르면 25px·15° 로 크게
  움직입니다(전에는 반대였습니다). 큰 이동은 마우스가 잘하고 키보드는 마지막 몇 px 을
  정확히 맞추는 데 강한데, 그 강점을 쓰려면 매번 `Shift` 를 쥐어야 했습니다. 개체 종류와
  무관하게 `Shift` 의 뜻이 하나라는 규칙은 그대로입니다. 도움말의 단축키 목록도 함께
  바뀝니다.

- **★ [드릴 정보]가 오른쪽 기능 바로 내려오고, 편집·시연이 다른 아이콘을 씁니다** — 전에는
  드릴 편집과 시연이 헤더 제목 옆의 **같은 ⓘ 하나**를 나눠 썼습니다. 눌러서 창이 뜨고 입력
  칸이 없는 것을 보고서야 "여긴 읽기 전용이구나" 를 알았습니다. 이제 두 화면 모두 오른쪽
  세로 기능 바에 [정보] 칸이 서고, 아이콘이 갈립니다: **편집 화면은 연필**(고칠 수 있습니다),
  **시연 화면은 눈**(볼 수만 있습니다). 밑판(정보 카드)은 같아서 두 화면을 오갈 때 같은
  기능으로 읽힙니다. 자리는 두 바 모두 **맨 끝**이라 기존 칸들의 위치는 그대로입니다.

- **★ [비우기]가 [보드 설정] 모달로 들어갔고, 드릴 편집에도 생겼습니다** — 전술판에서
  [비우기]는 오른쪽 기둥이 아니라 [보드 설정] 안, **잠금 설명 바로 밑**에 있습니다.
  "코트를 바꾸려면 먼저 판을 비우세요"라고 말하는 자리와 그 말대로 할 손잡이가 같은 곳에
  있어야 하니까요. 판이 이미 비어 있으면 버튼은 꺼져 있습니다.

  **드릴 편집에도 [비우기]가 생겼습니다.** 뜻은 하나입니다 — **지금 스텝만** 비웁니다.
  다른 스텝은 그대로이고, 되돌리기 **한 번**이면 전부 돌아옵니다(개체 수만큼 누르지
  않습니다). 확인 창이 그 범위를 그대로 적어 줍니다. 드릴의 코트는 만들 때 정해지므로
  비워도 형태·크기는 바뀌지 않고, 그 사실도 모달에 적혀 있습니다.

- **★ 전술판의 [비우기]를 되돌릴 수 있습니다** — 전에는 비우고 나면 되돌리기 이력이 통째로
  사라져서, 실수로 비운 판을 되살릴 방법이 없었습니다. 이제 [비우기]는 다른 편집과 똑같이
  한 칸 쌓이고, [되돌리기](또는 Ctrl+Z)로 그대로 돌아옵니다. 비우기가 코트(형태·크기)와
  제목을 유지하는 것도 그대로입니다.

  **코트 형태·크기를 바꾸는 것도 되돌릴 수 있습니다** — 되돌리면 코트까지 함께 돌아옵니다.
  코트를 바꾸려면 판이 비어 있어야 해서 [비우기]와 코트 전환은 붙어 다니는데, 전에는 전환이
  히스토리를 비워서 방금 지운 배치를 영영 못 되살렸습니다. 이제 `그리기 → 비우기 → 코트
  전환` 뒤에 두 번 되돌리면 그리던 판이 그 코트로 그대로 돌아옵니다.

  같이 정확해진 것: **코트 형태·크기를 언제 바꿀 수 있는지**의 판정이 "되돌릴 편집이
  없는가"에서 **"판 위에 개체가 하나도 없는가"**로 바뀌었습니다. 그래서 ①비우기 직후에는
  바꿀 수 있고 ②그걸 되돌리면 다시 잠기며 ③개체를 놓은 판을 저장했다 다시 열어도 잠긴
  채입니다(전에는 이 마지막 경우를 맞히려고 별도 표식을 저장본에 싣고 다녀야 했습니다).

- **★ 전술판의 [저장]도 이름을 묻고, 저장하면 바로 그 드릴 편집으로 갑니다** — 전에는
  누르는 즉시 판에 붙어 있던 제목(없으면 `새 드릴`)으로 저장하고 [목록에서 보기] 토스트만
  띄웠습니다. 이름을 안 붙인 판이 전부 `새 드릴`로 쌓였고, 목록에 가서야 그걸 알았습니다.
  이제 이름 칸에 판의 제목이 실려 뜨고(전체 선택돼 있어 그대로 덮어쓸 수 있습니다),
  [저장]을 누르면 만들어진 드릴의 편집 화면이 곧장 열립니다. 코트는 묻지 않습니다 —
  판이 이미 들고 있으니까요. **전술판은 그대로 남습니다**(승격은 복사입니다).

- **★ [새 드릴]이 이름과 코트부터 묻습니다** — 전에는 목록이나 헤더에서 [새 드릴]을 누르면
  빈 자유 전술판이 열렸고, 거기서 그린 뒤 [드릴로 저장]을 눌러야 드릴이 됐습니다. 이제
  드릴 이름과 코트(형태 · 풀 코트일 때는 크기 3단)를 고르는 창이 뜨고, [만들기]를 누르면
  스텝 하나짜리 드릴이 **빈 판으로** 만들어져 편집 화면이 열립니다. 기본 포메이션을 미리
  깔지 않는 이유는 자유 전술판과 같습니다 — 놓는 건 [포메이션으로 채우기]가 한 번에 하지만,
  치우는 건 한 번이 아닙니다. 이름은 비워 둬도 됩니다 —
  `새 드릴`로 만들어지고 인스펙터에서 언제든 고칩니다. **즉흥으로 그리다 남기는 길은
  그대로입니다**: 전술판에서 그린 뒤 [드릴로 저장]을 누르면 됩니다. 두 길의 차이는
  "드릴을 만들 작정으로 시작하느냐"입니다.

- **★ 판 관련 조작이 [보드] 버튼 하나로 모였습니다** — 전에는 [코트]는 모달, [골대]는 즉시
  실행, [속도]는 즉시 토글, [보기]는 서랍으로 제각각이라 어디를 눌러야 할지 외워야 했습니다.
  이제 오른쪽 기둥의 [보드]를 누르면 코트 형태·크기·진영, 격자와 골 지역 표시, 개체 이동 속도
  제한, 골대 원위치가 한자리에 있습니다. **격자를 켜고 끄려면 이제 모달을 열어야 합니다** —
  버튼이 넷에서 하나로 줄어든 대신 치른 값입니다. [골대 원위치]는 누르면 모달이 닫힙니다
  (결과가 판에 있으니까요).
- **메모 도구가 서랍 없이 바로 눌립니다** — [설명] 손잡이를 눌러 서랍을 연 뒤 메모를 고르던
  것이, 이제 트레이에서 [메모]를 바로 누릅니다. 자리는 그대로입니다.
- **좁은 화면에서 코트가 조금 넓어졌습니다** — 오른쪽 기둥의 버튼이 줄면서 1024×600 같은
  창에서 기둥이 두 줄이 아니라 한 줄로 서고, 그만큼 코트가 넓게 그려집니다.
- **★ 인쇄에도 원·화살표·구역 나눔·구역 번호가 나옵니다** — 종이에는 격자(구역 선과 번호),
  골에어리어 규칙 표시, 공의 3m·5m 원, 세트피스 소유 화살표가 빠져 있었습니다. 화면에서 켠
  것이 그대로 인쇄됩니다. 격자는 종이에서 잘 보이도록 화면보다 진하게 찍습니다.
- **★ 내보낼 스텝을 고를 수 있습니다** — 그림(PNG)과 인쇄 모두 [이 스텝] · [선택한 N장] ·
  [전체 N장] 중에서 고릅니다. 스텝 사이드바에서 체크해 둔 것이 있으면 그게 기본으로 잡힙니다.
  **여러 장을 고르면 ZIP 한 파일로 받습니다**(풀면 `_01`, `_02`처럼 번호가 붙은 PNG들이 나오고, 두 자리라 파일 탐색기에서도 순서대로 정렬됩니다). 한 장이면
  전처럼 PNG 그대로고요. 인쇄도 같은 범위를 따르며, 3·5번만 뽑아도 종이에는 원래 번호인 3, 5로
  찍혀 판과 짝을 지을 수 있습니다.
- **드릴 편집에서 [그림]이 항상 첫 스텝만 내보내던 문제를 고쳤습니다** — 3번 스텝을 보면서
  눌러도 1번 스텝이 저장됐습니다. 이제 보고 있는 스텝이 나갑니다.
- **★ 5m 원을 켜면 어느 팀 공인지 화살표로 보입니다** — 공을 가로지르는 2m 길이의 흐린 흰색
  화살표가 그 팀이 공격하는 방향을 가리킵니다(심판이 팔로 방향을 가리키는 것과 같은 뜻입니다).
  공을 다시 누르면 우리 공 → 상대 공 → 원 없음으로 넘어갑니다. 전에는 소유를 진영으로만
  판단해서, 코너킥처럼 공격이 차는 재개는 맞았지만 **골킥처럼 수비가 차는 재개는 5m를
  물러나야 할 팀이 반대로 나왔습니다** — 그걸 고치려면 진영을 뒤집어야 했고 그러면 골에어리어
  3인 판정까지 같이 뒤집혔습니다. 이제 둘이 따로 움직입니다. 시연 화면과 내보낸 그림에도
  같은 화살표가 나갑니다.
- **★ 공의 거리 원(3m·5m)이 이제 스텝마다 따로 저장됩니다** — 전에는 한 번 켜면 그 드릴의
  모든 스텝에서 켜져 있었습니다. 이제 공을 다시 눌러 원을 바꾸면 **지금 편집 중인 스텝만**
  바뀌므로, "킥오프를 기다리는 동안에는 5m 원, 공을 찬 다음 스텝부터는 원 없음" 같은 장면을
  만들 수 있습니다. 스텝마다 켜려면 그 스텝에서 각각 눌러 주셔야 합니다. **기존 드릴은 켜
  두셨던 원이 모든 스텝에 그대로 옮겨지므로 지금 보이는 그림이 달라지지 않습니다** — 다만
  이 판으로 만든 파일은 이전 버전 앱에서는 열리지 않습니다.
- **★ 내보낸 파일 이름만 봐도 어느 화면에서 여는지 알 수 있습니다** — 전에는 드릴이든 세션이든
  기기 이사 파일이든 전부 `.spin.json` 하나로 끝나서, 나중에 다운로드 폴더에서 보면 어느 것이
  무엇인지 구분할 수 없었습니다. 이제 종류가 확장자에 실립니다: 드릴은
  `SPIN_드릴이름_20260826.spin.drill.json`, 세션은 `.spin.session.json`, 기기 이사 파일은
  `SPIN_backup_20260826.spin.backup.json`. 파일 선택창도 그 화면에서 쓰는 종류를 먼저
  보여줍니다. **이전에 내보낸 `.spin.json` 파일은 그대로 열리니 다시 뽑지 않아도 됩니다** —
  파일 종류는 이름이 아니라 파일 안을 보고 판별하기 때문입니다.
- **파일을 엉뚱한 화면에 넣어도 어디로 가야 하는지 알려줍니다** — 드릴 파일이나 세션 파일을
  설정 화면의 [데이터 가져오기]에 넣으면 "지원하지 않는 파일 종류입니다" 라고만 떠서, 파일이
  잘못된 줄 알기 쉬웠습니다. 이제 "드릴 파일은 [드릴 목록]의 [가져오기]에서 엽니다" 처럼 갈
  곳을 짚어 줍니다. 반대 방향(드릴 목록에 기기 이사 파일을 넣는 경우)은 전부터 되던 것이라,
  이제 두 화면이 서로를 가리킵니다.
- **기본 주소가 spin.atit.app 으로 바뀌었습니다** — 예전 주소 spin.atit.dev 는 새 주소로
  자동 이동(301 리다이렉트)합니다. 즐겨찾기·공유해 둔 링크를 그대로 쓰셔도 됩니다.

## [0.5.0] 2026-08-21

### 추가됨

- **★ 구글 드라이브 동기화 (선택)** — 설정의 [Google Drive 동기화]에서 본인 구글 계정을 연결하면 드릴·세션·선수 명단이 여러 기기에서 함께 쓰입니다. 저장 위치는 **회원님 본인 Drive의 앱 전용 공간**이고 SPIN 서버는 없습니다 — 로컬이 언제나 원본이라 인터넷이 없는 체육관에서도 전부 그대로 동작하고, 연결이 돌아오면 자동으로 밀어올립니다. 같은 드릴을 두 기기에서 고쳤으면 나중에 고친 쪽이 남고, 한 기기에서 지운 드릴은 다른 기기에서도 지워집니다(지운 뒤 다른 기기에서 더 나중에 고쳤다면 고친 쪽이 살아납니다 — 가장 최근 활동이 이깁니다). 기본은 꺼짐이고, 켤 때 무엇이 어디로 가는지 동의 화면이 먼저 묻습니다. [연결 해제]는 이 기기의 데이터를 건드리지 않고, [Drive 데이터 삭제]는 Drive 쪽만 지웁니다. 설정과 자유 전술판은 동기화하지 않습니다 — 테마·글자 크기 같은 것은 기기마다 다른 게 자연스럽고, 통째로 덮으면 접근성 설정이 조용히 꺼지는 사고가 되기 때문입니다.
- **★ 한국어·영어·일본어 다국어 지원** — 브라우저 언어를 자동 감지해 셋 중 하나로 화면을 보여주고, 설정 화면 맨 위 [언어] 섹션에서 자동/한국어/English/日本語 중 직접 고를 수도 있습니다. 매칭되는 언어가 없으면 영어로 엽니다. 버튼·라벨·헤더·토스트·안내 문구부터 인쇄물(드릴 시트·세션 계획서)·PNG 내보내기까지 화면에 보이는 문구 전부가 대상입니다 — 드릴 제목처럼 직접 입력한 내용과 예시로 딸려 오는 시드 드릴은 그대로 둡니다.
- **드릴 편집에서 팀 이름을 고칩니다** — ⓘ [드릴 정보] 시트에 우리 팀·상대 팀 이름 입력칸이 생겼습니다. 코트 위 칩 이름표·시연 자막·[코트] 진영 문구가 전부 이 이름을 따라갑니다.
- **세션에 메모와 휴식 시간을 적습니다** — 세션 편집 화면에 세션 전체 메모, 그리고 각 드릴 옆에 메모·휴식 시간(분)을 적는 자리가 생겼습니다. 인쇄한 훈련 계획서의 빈칸이 채워집니다.
- **★ 삭제 안전망 — 스텝·드릴·세션·세션 편성 전부** — 이제 무엇을 지우든 되돌릴 방법이
  있거나, 지우기 전에 한 번 물어봅니다. 드릴 편집에서 스텝 한 장을 지우면 화면 아래
  토스트에 [되돌리기]가 뜹니다(Ctrl/⌘+Z 로도 돌아갑니다) — 선택모드로 여러 장을 한 번에
  지울 때만 지우기 전에 먼저 확인합니다. 드릴 목록에서 드릴을 지울 때는 이제 항상 확인을
  거치고, 8초짜리 [되돌리기] 토스트가 뜹니다(어느 세션에서 쓰이는지도 여전히 보여줍니다).
  세션 목록의 세션 삭제도 마찬가지로 확인 + 8초 [되돌리기]가 새로 생겼습니다(전에는 아무
  안전망이 없었습니다). 세션 편집에서 구획을 지우거나 편성한 드릴을 뺄 때도 이제
  [되돌리기] 토스트가 뜹니다 — 구획을 지워도 그 안의 드릴 편성은 이웃 구획으로 옮겨지지,
  버려지지 않습니다.
- **선수 실명이 인쇄물·그림에도 실립니다** — 실명을 적은 선수가 있으면 드릴 시트 첫 장과 PNG 그림 하단에 명단이 나옵니다(시연 화면에서 보이던 것과 같습니다). 아무도 실명을 안 적었으면 지금처럼 아무것도 안 나옵니다.
- **메모 글자 크기와 색을 고릅니다** — 메모 쓰기/수정 화면에 글자 크기(작게·보통·크게)와 색(흰색·하늘·노랑·빨강) 선택이 생겼습니다.
- **★ 화면마다 첫 방문 튜토리얼과 도움말이 생겼습니다** — 드릴 편집·자유 전술판·드릴 목록·세션 목록·세션 편집·시연, 6개 화면 전부 처음 열면 화면이 어두워지고 중요한 자리를 한 곳씩 밝혀 가며 설명합니다. 한 번 보면 다시 안 뜨고, 왼쪽 레일(좁은 창에서는 헤더)의 [도움말]을 누르면 지금 화면을 설명하는 문서형 도움말이 열립니다 — 안에 [이 화면 투어 다시 보기] 버튼도 있습니다. 설정 맨 아래 [도움말·튜토리얼] > [모두 다시 보기]를 누르면 모든 화면의 튜토리얼을 처음부터 다시 봅니다.

### 변경됨

- **★ 드릴 편집과 시연이 같은 레이아웃이 됐습니다** — 두 화면 모두 위에 컴팩트 헤더(드릴
  이름 · ⓘ · 전환 버튼), 오른쪽에 세로 기능바, 아래에 노트와 재생 버튼 묶음이 같은 자리에
  섭니다. 화면을 오갈 때 손이 기억한 자리가 그대로 맞습니다. 재생 버튼 묶음(반복·이전·
  재생·다음·배속)도 두 화면이 같은 모양이고, 마지막 스텝에서 [재생]을 누르면 처음으로
  되감아 다시 재생합니다(반복 설정과 무관).
- **시연 화면 오른쪽에 세로 기능바가 생겼습니다** — 코트 위에 떠 있던 전체화면·도움말
  버튼이 편집 화면과 같은 형식(아이콘+이름)의 오른쪽 바로 들어갔습니다. 그만큼 하단 재생
  버튼 위치가 창 크기와 무관하게 고정됩니다. 시연 진행바는 재생 버튼 옆에서 남는 폭을
  씁니다. 우상단의 [시연 종료] X 버튼은 없어졌습니다 — Esc 로 나가는 길은 그대로입니다.
- **[편집으로] 버튼에 펜 아이콘이 붙었습니다** — 시연 헤더 우측의 전환 버튼이 글자만으로도,
  아이콘만으로도 읽힙니다.
- **드릴 편집 오른쪽 기능바에서 [저장] 칸이 빠졌습니다** — 드릴은 자동으로 저장되므로
  누를 이유가 없는 버튼이었습니다. 자유 전술판의 [드릴로 저장]은 그대로 있습니다.
  칸 하나가 빠지면서 1024×600 같은 좁은 화면에서 기능바가 한 열로 줄어 코트가 44px 더
  넓어집니다.
- **드릴 편집 하단 노트가 한 줄을 아끼게 됐습니다** — "노트" 레이블이 왼쪽에 작게 붙고,
  메모 미리보기·입력칸이 그 바로 오른쪽에서 시작합니다.
- **JSON 내보내기가 설정으로 이사했습니다** — 드릴 편집의 [내보내기]에는 그림(PNG)과
  인쇄만 남고, 전체 데이터 백업은 설정 > [데이터] > [데이터 내보내기]에서 합니다.
  "기기 이사 파일 읽기"는 "데이터 가져오기"로 이름이 바뀌었습니다.
- **드릴 목록 카드가 더 촘촘해졌습니다** — 카드 하나의 최소 폭을 2/3로 줄여, 같은 화면
  폭에서 한 줄에 늘어서는 카드 수가 늘었습니다.

### 제거됨

- **설정의 [기본 포메이션]·[기본 코트 모드] 행을 없앴습니다** — 포메이션은 코치의 재량이라
  앱이 기본값을 정해 두지 않습니다([포메이션으로 채우기]는 표준 배치로 깔아 주고, 거기서
  코치가 옮깁니다). 시작 코트는 [보드] 전술판이 마지막 상태를 스스로 기억하기 때문에 이
  설정은 사실상 첫 실행 한 번만 쓰였고, 그 뒤로는 바꿔도 화면이 따라오지 않았습니다 —
  코트는 판 위의 코트 전환으로 바꾸면 즉시, 영구히 반영됩니다. 예전에 저장된 설정값이
  있어도 조용히 무시될 뿐, 드릴·세션 등 다른 데이터에는 아무 영향이 없습니다.
- **설정의 [팀] 섹션(팀 색상 선택)도 없앴습니다** — 팀 색은 판을 만들 때 찍히는 값이라,
  설정에서 바꿔도 이미 만든 드릴·전술판에는 적용되지 않는 반쪽짜리 기능이었습니다. 이제
  팀 색은 표준 팔레트(빨강/파랑)로 고정되고, 이미 만든 드릴은 지금 색 그대로 유지됩니다.
  팀 **이름**은 전처럼 드릴 편집의 ⓘ [드릴 정보]에서 판마다 고칠 수 있습니다.

### 수정됨

- **썸네일에서 도형과 겹친 화살표가 안 보이던 것** — 스텝 카드·드릴 목록의 작은 코트
  그림에서 화살표가 사각형·타원 위를 지나면 사라져 보였습니다. 판·인쇄·PNG 와 같은 검정
  테두리(케이싱)를 썸네일 화살표에도 깔아 어떤 배경 위에서도 또렷이 읽힙니다.
- **드릴 편집 스텝 목록의 선택모드 UI가 출렁이던 것** — 선택모드를 켜면 카운트·복제·삭제 줄이 새로 생기며 카드 목록을 아래로 밀어냈습니다. 이제 그 자리는 켜짐/꺼짐과 무관하게 항상 같은 높이라 목록이 흔들리지 않습니다. 스텝 카드를 길게 눌러 [선택] 메뉴를 여는 것도 이제 터치에서 됩니다 — 지금까지는 마우스 오른쪽 클릭 전용이었습니다.

## [0.3.0] 2026-08-19

### 추가됨

- **★ 드릴 라이브러리에 목록 보기가 생기고 썸네일이 절반이 됐습니다** — 카드의 코트 그림
  세로가 1/2 로 줄어 한 화면에 더 많은 드릴이 보이고, [카드/목록] 토글로 썸네일 없는 한 줄
  목록으로도 볼 수 있습니다(행에서도 열기·시연·복제·내보내기·삭제 전부 됩니다).
- **★ [드릴 정보]가 하단 노트 왼쪽 ⓘ로 오고, 화면 중앙 모달이 됐습니다** — 우상단 구석
  대신 노트 패널 왼쪽의 ⓘ 버튼이 열고, 창이 화면 가운데(2/3 크기)에 떠서 닫기 [×]가
  스크롤과 무관하게 항상 보입니다.
- **★ 시연의 [편집으로]가 정말 편집으로 갑니다** — 지금까지는 이력 뒤로가기라, 목록에서
  시연에 들어왔으면 [편집으로]를 눌러도 목록이 떴습니다. 이제 그 드릴의 편집 화면으로 바로
  가고, 세션 시연에서는 [세션으로]가 그 세션의 편집 화면으로 갑니다. [시연 종료]·Esc 는
  이전처럼 들어온 자리로 돌아갑니다.
- **★ 드릴 편집 하단 최우측에 [시연] 버튼** — 넓은 화면의 드릴 편집은 헤더가 없어 시연으로
  가는 문이 좁은 화면에만 있었습니다. 하단 노트 줄 오른쪽 끝에서 한 번에 시연을 엽니다.
- **★ 시연 중에도 드릴 정보를 봅니다** — 시연 화면 우상단 ⓘ를 누르면 목적·코칭 포인트·
  장비 등이 읽기 전용 모달로 뜹니다.
- **★ 시연 화면의 메모가 편집 화면과 같은 쪽지로 나옵니다** — 지금까지는 글자만 떠 있어
  어두운 코트에서 잘 안 읽혔습니다. 격자도 편집에서 켜 둔 설정 그대로 시연에 나옵니다.
- **★ 드릴 분류가 코칭 표준 두 축으로 바뀌었습니다** (구조 개편 1차, 드릴 스키마 v8) —
  자유 입력 카테고리 대신 **유형**(기술·전술·세트피스·경기 상황·컨디셔닝)과 선택적
  **경기 상황**(킥오프·킥인·골킥·코너킥·직접/간접 프리킥·페널티킥·세트볼·오픈 플레이·
  2대1 스페이싱 — FIPFA Laws 2025 의 재개 어휘)을 씁니다. 드릴 서술도 USPSA 관행에 맞춰
  목적(Purpose)·진행 방법(Setup)·**변형(Variation, 신설)** 3필드로 정리했습니다.
  ⚠️ 예전 드릴은 자동 변환됩니다 — 옛 카테고리 문자열은 태그로 보존되고, 폐기된
  훈련량(반복·세트·인터벌) 값은 설명 말미에 "훈련량(구버전): …" 으로 남습니다.
  목록 필터·검색·인쇄·세션 분류 점이 전부 새 축을 따릅니다.
- **★ 세션에 구획(phase) 계층이 생겼습니다** (구조 개편 1차, 세션 스키마 v2) — 코칭 표준의
  세션 짜임(워밍업→기술→전술→세트피스→스크리미지→쿨다운, +자유 구획)대로 드릴을 구획에
  나눠 담을 수 있습니다. 구획마다 목표 배분 시간, 세션 전체에 목표 총 시간을 적을 수
  있습니다(강제하지 않고 배분 표시의 기준선일 뿐입니다). 기존 세션은 '훈련' 구획 하나로
  자동 승격되며 잃는 것이 없습니다. 구획을 편집하는 전용 화면은 다음 단계에서 옵니다 —
  지금의 세션 드로어는 이전처럼 평평한 목록으로 동작합니다.
- **선수 명단(로스터)의 토대가 놓였습니다** (구조 개편 1차) — 이름과 PF1/PF2 클래스를 담는
  단일 팀 명단이 저장소·백업에 합류했습니다. 전체 백업에 명단이 함께 실리고, 복원은
  기본적으로 **이 기기 명단이 비어 있을 때만** 덮습니다(남의 백업에서 드릴만 받아도 내 팀
  명단이 안전합니다). 명단을 편집하는 화면과 세션 참가자 체크는 3차에서 옵니다.
- **★ 화면에 주소가 생겼습니다** (구조 개편 1차, react-router 도입) — 드릴 편집·시연·목록이
  각각 해시 주소(`#/drills/…`, `#/present/…`)를 가져, **새로고침해도 보던 화면과 대상이
  그대로 복원**됩니다(전에는 체육관 태블릿이 시연 중 리로드되면 빈 화면이 떴습니다).
  뒤로가기 규칙(앱 안 이력이 있으면 뒤로, 없으면 홈)과 단축키·레일 동작은 이전과 같습니다.
- **★ 세션이 왼쪽 레일의 1급 화면이 됐습니다** (구조 개편 2차) — 드릴 목록의 2번째 탭에
  숨어 있던 세션이 [보드]·[드릴]과 동급의 [세션] 항목으로 나왔습니다(`#/sessions`).
  헤더의 [새 세션]으로 언제든 만들 수 있고(만들자마자 편집 화면이 열립니다), 편집 중인
  세션도 주소에 실려 새로고침·뒤로가기에서 그대로 복원됩니다. 세션을 시연하는 동안
  레일 활성도 [세션]입니다. 드릴 라이브러리는 드릴 전용으로 홀가분해졌습니다.
- **★ 세션 전용 편집 화면이 생겼습니다** (구조 개편 2차) — 좁은 드로어 대신 전체 화면
  (`#/sessions/세션id`)에서 세션을 짭니다. 구획(워밍업·기술·전술·세트피스·스크리미지·쿨다운·
  자유)을 만들어 순서를 정하고, 구획마다 드릴을 편성하고 목표 배분 시간을 적으면 소계와
  초과가 바로 표시됩니다. 세션 전체의 목표 시간 대비 게이지도 상단에 섭니다(어느 것도
  저장을 막지 않습니다 — 색으로만 알립니다). 구획을 지워도 편성한 드릴은 사라지지 않고
  이웃 구획으로 옮겨 붙습니다. 드릴을 다른 구획·다른 순서로 옮기는 ↑↓, 시간 덮어쓰기,
  시연·내보내기까지 드로어가 하던 일을 전부 이어받았습니다.
- **★ 드릴 정보를 편집 화면에서 바로 고칩니다** (구조 개편 2차) — 드릴 편집 화면 오른쪽
  위의 ⓘ [드릴 정보] 버튼이 시트를 엽니다. 유형·경기 상황·난이도·소요 시간·태그, 서술
  3필드(목적·진행 방법·변형), 그리고 0.2.1 에서 [속성] 폐기로 고칠 자리가 없다던 코칭
  포인트·필요 인원·필요 장비가 전부 여기서 편집됩니다. 고친 값은 다른 편집과 똑같이
  자동저장·되돌리기(Ctrl+Z)를 탑니다.
- **★ 선수 명단과 세션 참가자 체크가 생겼습니다** (구조 개편 3차) — 설정 > [선수 명단]에서
  이름과 PF 클래스(PF1 중증·PF2 경증, 미분류 가능)를 등록하면, 세션 편집 화면에 참가자
  체크리스트가 나타납니다. 체크된 인원과 PF2 수를 보여주되 참가를 제한하지는 않습니다
  (동시 출전 2명 제한은 경기 라인업의 일이고, 판단은 코치 몫입니다). 명단은 전체 백업에
  함께 실립니다.
- **★ 시연이 구획을 압니다** (구조 개편 3차) — 구획이 둘 이상인 세션을 시연하면 하단에
  현재 구획("워밍업 1/2")이 표시되고, 구획 경계를 넘는 드릴 전환에서는 쉼 화면이 "다음
  구획: 전술"처럼 구획 이름을 알립니다. 상단 세션 진행 막대에도 구획 경계마다 틈이
  벌어집니다. 구획이 하나뿐인 세션은 이전과 완전히 같습니다.
- **드릴 라이브러리에 상황 필터와 정렬이 생겼습니다** (구조 개편 3차) — 유형 필터 옆에서
  경기 상황(킥인·코너킥 등)으로 거르고, 최근 수정순·만든 순·이름순으로 정렬합니다
  (정렬은 저장소에 이미 있던 기능의 UI 노출입니다).

- **★ 스텝 편집을 왼쪽 세로 바로 다시 짰습니다** — 목록·현재 스텝·이동을 왼쪽 `StepSidebar`
  가 전담합니다. 카드는 번호+썸네일만 보여주고, 좁은 화면에서는 자동으로 접혔다가 버튼으로
  열면 보드 위에 겹쳐 뜹니다(전용 좁은 화면 UI 를 따로 두지 않았습니다). 카드 재정렬은
  드래그와 방향키 둘 다 됩니다.
- **★ 스텝 복제·연결·다중 선택이 쉬워졌습니다** — 카드의 복제 버튼은 바로 아래로 복제하고,
  스텝 사이 틈에 뜨는 **+** 는 그 자리에 위 스텝의 복제를 끼워 넣습니다(맨 앞 틈은 첫 스텝
  복제). 틈의 **사슬 토글**로 두 스텝의 연결을 끊으면, 재생·시연에서 그 경계만 멈추지 않고
  즉시 컷됩니다. 선택 모드를 켜면 카드에 체크박스가 나타나 여러 스텝을 묶어 드래그로 옮기고
  한 번에 복제·삭제할 수 있고, 되돌리기(Ctrl+Z)는 그 일괄 작업 전체를 한 번에 되돌립니다.
- **★ 스텝 노트 패널** — 보드 아래 접이식 패널에서 선택한 스텝의 메모를 씁니다(기본은
  접힌 채, 내용이 있으면 첫 줄을 미리 보여줍니다). 드릴의 짧은 소개는 편집 화면 헤더에서
  제목 옆을 눌러 바로 고칠 수 있고, 목록 카드에 부제로도 뜹니다.
- **★ 목록 카드·스텝 칩에 작도 도형과 메모가 나옵니다** — 지금까지 썸네일은 휠체어·공·콘·
  화살표만 그렸습니다. 도형은 판과 **같은 코드**로 그리므로 반투명 농도가 어긋날 일이 없고,
  메모는 쪽지 모양 그대로 글자까지 작게(카드에서 약 3 px) 실립니다.
  ⚠️ **도형은 테두리만 굵게 그립니다** — 크기를 키우면 코치가 그리지 않은 구역을 가르칩니다.
  ⚠️ 예전에 저장한 드릴의 카드도 함께 채워집니다 — 목록을 처음 열 때 옛 요약을 **한 번**
  다시 만듭니다(그 드릴이 많으면 첫 목록이 조금 늦게 뜹니다).
- **★ 스텝 카드 우클릭 메뉴** — 왼쪽 바의 스텝 카드를 오른쪽 버튼으로 누르면 선택(선택
  모드 시작)·아래로 복제·위로 복제·삭제 메뉴가 뜹니다. 마지막 한 장은 삭제가, 60장 정원에
  차면 복제가 잠깁니다.
- **★ 화살표에 회전 앵커가 생겼습니다** — 화살표를 선택하면 곡선 한가운데에서 실로 이어진
  강조색 점이 하나 더 뜹니다(굽힘 반대쪽이라 굽힘점과 안 겹칩니다). 끌면 화살표가 잡는 순간의
  한가운데를 축으로 통째로 돌고, 모양·화살촉·색은 그대로 유지됩니다.
- **★ 보드의 도형·메모·화살표를 우클릭 메뉴로 복제합니다** — 작도 도형·메모·화살표를
  오른쪽 버튼(또는 길게 누르기)으로 열면 [복제] 항목이 뜨고, 사본이 원본의 **오른쪽 아래
  0.5 m** 에 놓이며 선택이 사본으로 옮겨가 바로 끌어 자리 잡을 수 있습니다. 여럿을 골라 함께
  복제할 수도 있습니다(이 셋만 고른 경우). 판 가장자리에서는 사본이 판 밖으로 나가지 않게
  멈추고, 화살표는 모양이 찌그러지지 않게 이동량만 줄어듭니다. **`Ctrl/⌘+D` 로도 됩니다** —
  복제 가능한 개체를 골라 두었으면 그 개체들을, 아니면 종전대로 현재 스텝을 복제합니다.
- **★ 드릴 이름을 편집 화면에서 바로 고칩니다** — 왼쪽 스텝 바 **맨 위**의 이름을 누르면
  그 자리가 입력칸이 됩니다(Enter/포커스 이동 = 저장, Esc = 취소). 빈 이름은 저장되지
  않습니다. 좁은 화면에서는 헤더의 이름도 같은 방식으로 고칠 수 있습니다.

### 변경됨

- **★ 편집 화면 하단이 노트 하나로 정리됐습니다** — 스텝 칩 줄은 왼쪽 `StepSidebar` 로
  옮겼고, 남아 있던 하단 재생 바마저 없앴습니다. 재생 버튼과 배속(0.5×→1×→2×)은 왼쪽
  스텝 바 **하단**에 있습니다. 개체 이동 속도 제한은 오른쪽 기능 바의 [속도] 그대로입니다.
- **★ 왼쪽 스텝 바가 날씬해졌습니다** — 카드(썸네일)가 2/3 크기(폭 134px)가 되어 코트가
  그만큼 넓어졌고, 대신 카드 안 개체는 배율을 올려 같은 크기로 보입니다. 스텝 사이 틈의
  +·사슬 버튼은 두 배(32px)로 커져 터치로 누르기 쉬워졌습니다. 하단 바가 사라진 만큼
  좁은 화면에서 코트가 세로로 19px 더 커집니다.
- **★ 스텝 이름을 폐기하고, 있던 내용은 노트로 옮겼습니다** — 인스펙터의 이름 입력칸이
  사라지고, 옛 드릴에 이름이 있던 스텝은 여는 순간 자동으로 그 이름을 노트 맨 앞줄로
  합쳐 보존합니다(내용 유실 없음). 자동으로 붙던 '스텝 N' 같은 빈 이름은 버립니다.
  ⚠️ **예전에 저장한 드릴의 목록 카드도 함께 다시 만듭니다** — 목록을 처음 열 때 옛 요약을
  한 번 재구축하므로, 드릴이 많으면 첫 목록이 조금 늦게 뜰 수 있습니다.
- **★ 목록 카드·스텝 칩의 개체가 훨씬 커졌습니다** — 휠체어·공·콘·화살표를 판의 두 배 크기로
  그립니다(휠체어 원의 지름이 약 2 m). 지금까지는 축척을 지키다가 카드에서는 점,
  **44 px 스텝 칩에서는 1 px 미만**이 되어 사진 뭉치가 빈 코트처럼 보였습니다. 칩은 카드보다
  4배 가까이 작으므로 배수를 한 번 더 얹습니다.
  ⚠️ **커진 것은 개체 그림뿐이고 자리는 그대로입니다** — 자리까지 부풀리면 썸네일이 실제와
  다른 배치를 보여주게 됩니다.
- **★ 썸네일에서 화살표가 도형에 묻히지 않습니다** — 화살표 선을 굵히고(카드에서 약 3.6px)
  도형 테두리 과장을 줄여, 도형 위를 지나는 화살표 경로가 선명하게 읽힙니다.

## [0.2.1] 2026-08-17

0.2.0 을 코트에서 써 보고 주신 지적들입니다.

### 추가됨

- **★ 세트피스 5 m 제한** — 공의 원을 **5 m 로 켜면** 그 공은 세트피스(골킥·킥인·킥오프·
  프리킥·코너킥·페널티킥)라는 뜻입니다. 그때 **수비가 원에 조금이라도 걸치면** 2-on-1 처럼
  링이 붉어지고 음성으로 알립니다. 수비 골키퍼는 **자기 골라인을 완전히 넘어가** 있으면
  제한을 받지 않습니다 — 골라인 위 **6 m**(코트 크기와 무관) 안이어야 하고, 차체가 한 뼘이라도
  경기면에 남아 있으면 면제가 아닙니다. 누가 수비인지는 **풀 코트는 왼쪽 진영, 하프 코트는
  깃발 색 진영**.
  ⚠️ **5 m 원인 공에는 2-on-1 을 걸지 않습니다** — 세트피스는 아직 인플레이가 아니라
  2-on-1 이 성립하지 않습니다. 3 m 원과 원 없음은 전과 똑같이 2-on-1 로 잽니다.
- **2-on-1 골키퍼 면제에 자리가 하나 늘었습니다** — 전에는 **자기 골 지역 안**일 때만 인원에서
  빠졌는데, 이제 **자기 골라인을 완전히 넘어가** 포스트 사이(6 m)에 있어도 빠집니다.
  원문 대조 결과입니다: Law 11 의 면제 조항은 골 지역뿐이지만, 같은 Law 에 **경기장을 벗어나
  2-on-1 을 회피**하는 것을 허용하는 문단이 있고, Law 12 는 **골키퍼 외의 선수**가 자기 골라인을
  완전히 넘으면 그 자체를 간접 프리킥으로 다룹니다 — 그 자리에 합법적으로 있을 수 있는 선수가
  골키퍼뿐이라는 뜻입니다. 그래서 실제 경기에서 세지 않는 배치를 판이 붉게 물들이고 있었습니다.
  ⚠️ **세트피스 5 m 쪽에는 골 지역 면제가 없습니다** — 두 규칙의 면제 자리가 다릅니다
  (재개 상황의 골키퍼는 골문 안으로 물러나 있어야 합니다). 자세한 건 README 의 규칙 표.
- **선 색을 선 위에서 바꿉니다** — 굽힘점(가운데 앵커)을 거듭 누르면 **하늘 → 노랑 → 빨강**
  으로 돕니다. 굽힘점 자체가 지금 색으로 칠해집니다. 목록 카드와 스텝 사진의 썸네일도
  그 색으로 나옵니다.
  ⚠️ **노랑과 빨강은 적록색약에게 거의 같은 색입니다** — 뜻은 화살촉으로 가르고 색은 거드는
  용도로 쓰십시오.

### 수정됨

- **그림으로 내보내기에 작도 도형과 진영 표시가 안 나오던 것** — 화면에는 있는데 PNG 에만
  통째로 빠져 있었습니다.
- **인쇄에 진영 표시가 안 나오던 것**, 그리고 **코트가 용지의 절반만 쓰던 것** — A4 가로에서
  그림이 남는 자리를 전부 채웁니다.

### 변경됨

- **내보낸 그림의 배경이 검정**이 되고 아래 캡션 글자가 흰색이 됐습니다.

### 제거됨

- **★ [속성] 버튼과 속성 패널을 없앴습니다** — 드릴 이름은 헤더에서, 설명도 헤더에서,
  스텝 조작은 왼쪽 바에서 합니다. 이와 함께 속성 패널 안에만 있던 입력칸들(선수 이름,
  태그, 목표·장비·코칭 포인트 등 교육 정보, 스텝별 재생 간격, 배치 프리셋)은 당분간
  고칠 자리가 없습니다 — 이미 넣어 둔 값은 그대로 보존되고 화면·인쇄에도 계속 나옵니다.

## [0.2.0] 2026-08-17

0.1.0 이후 **74커밋**. 대부분 실제 코트에서 써 보고 주신 피드백이고, 여러 건은 앞 배포에서
*"이렇게 하지 않는다"* 고 적어 둔 결정을 뒤집은 것입니다. **뒤집은 근거는 커밋 메시지와 코드
주석에 그대로 남아 있습니다** — 여기는 무엇이 달라졌는지만 적습니다.

### 추가됨

- **작도 도형** — 원(타원)·정삼각형·사각형. 층은 코트 위·칩 아래, 겹치면 진해집니다.
  ⚠️ **드릴 파일이 v4 가 됩니다** — 도형이 든 드릴을 옛 버전 앱에서 열면 거절합니다.
- **개체 메뉴** (오른쪽 클릭 · 긴 터치) — 수정 · 잠금 · 무시 · 빼기/삭제. 스텝마다 따로입니다.
- **메모를 놓은 자리에서 바로 씁니다** — 놓자마자 글 칸이 열리고, 나중에는 두 번 누르거나
  메뉴 [수정]. **줄바꿈이 됩니다**(표시·인쇄·시연·내보내기 전부).
- **진영 깃발** — 골라인 밖 0.5 m 에 깃대와 페넌트. 공·콘과 안 헷갈리는 모양입니다.
- **코트 위 마우스 휠 줌** — 커서가 가리키던 자리가 제자리에 붙습니다.
- **공 거리 원 순환** — 다시 누르면 없음 → 3 m → 5 m → 해제. 공마다 따로 기억합니다.
- **전술판 세션 캐시** — 떠났다 돌아오면 같은 판입니다(되돌리기 이력까지).
- **새 앱 아이콘** — 로고·파비콘·홈 화면 아이콘을 하나로 통일.

### 변경됨

- **★ 판이 한 덩어리가 됐습니다** — 코트와 벤치를 테두리 하나로 묶고, 코트 양옆의 죽은 여백
  (1024×600 에서 한쪽 86 px)을 벤치가 가져갑니다. 코트 축척 손해 **0.00%**, 선수 칩 스크롤 소멸.
- **★ 자유 전술판 재설계** — 속성 패널을 없애고 앱 조작을 오른쪽 기능 기둥 하나로 모았습니다.
- **★ 트레이가 코트 긴 변에 붙습니다** — 가로면 아래 띠, 세로면 옆 기둥. 서랍은 뜨는 창으로.
- **넓은 화면에서 상단 헤더 제거** — 판이 62 px 을 돌려받습니다(좁은 화면은 헤더 유지).
- **★ 단축키 전면 개편** — 키맵 정본 하나 + `code` 기반 판정. 도움말이 그 표를 읽습니다.
  지우개 도구는 없앴습니다 — 개체를 없애는 길은 트레이 드롭 · 메뉴 · Delete 입니다.
- **★ 다중 선택 재설계** — 고무줄로 훑고(화살표·도형 포함), 하나를 끌면 통째로 갑니다.
- **배치는 1회용이 기본**, 연속으로 놓으려면 도구를 한 번 더 눌러 고정합니다.
- **Delete 하나로 통일** — 하나든 여럿이든 같은 키입니다(Ctrl+Delete 폐지).
- **트레이 드롭이 놓기 전에 결과를 말합니다** — "놓으면 빼기" / "놓으면 삭제".
  선수·공·콘은 트레이로 돌아가고, 화살표·메모·도형은 돌아갈 자리가 없어 삭제됩니다.
- **화살표는 선 하나**이고, 양 끝 화살촉을 반복 클릭으로 순환합니다.
- **★ 반칙을 점이 아니라 차체 사각형으로 잽니다** — 앞범퍼가 선을 밟으면 잡힙니다.
  ⚠️ **판정이 넓어져 전보다 자주 붉어집니다**(정면 4.2 m · 옆 3.5 m · 뒤 3.3 m 까지).
- **골 지역 채움이 붉은 2단**이 되고 **진영별로** 갈립니다 — 평소 연하게, 3인 반칙이면 진하게.
- **★ 진영이 생겼습니다** — 어느 팀이 어느 골대를 지키는지를 판이 알게 됐습니다. 그래서
  **골키퍼 면제가 자기 골 지역에서만** 되고, 골 지역 3인 반칙도 **지키는 팀만** 셉니다.
  전에는 팀↔골대 배정을 몰라 둘 다 넓게 쟀는데, 특히 후자가 위험했습니다 — 골 앞 마무리
  드릴은 **공격 3대**가 골 지역에 들어가는 것이 정상이라 그 판이 상시 붉었습니다.
- **센터 마크**를 페널티 스팟 크기로 키우고 **하프 코트에도** 찍습니다(규격 15 cm 가 아닙니다).
- **토스트가 화면 아래에서 위로** 갔습니다.
- 휠체어 칩 44×60 → **44×44**. 되돌리기·다시하기는 헤더에서 벤치 기둥으로.
- 레일 [보드]는 **언제나 자유 전술판**입니다 — 드릴 편집과 안 섞입니다.

### 고쳐짐

- 오른쪽 클릭이 왼쪽 클릭이 하는 일을 한 번 더 하던 것 — 이제 메뉴만 엽니다.
- 코트 위에서 브라우저(크롬) 메뉴가 뜨던 것.
- 잠근 개체가 다른 개체에 밀리던 것 · 잠김 덮개가 개체 밑에 그려지던 것.
- 무시된 개체를 고르지도 오른쪽 클릭도 못 하던 것.
- 작도·메모 서랍이 안 펼쳐지던 것(트레이가 자기 패널을 잘라내고 있었습니다).
- 아이패드 세로에서 판이 눕던 것.
- 전술판을 고치고 곧바로 화면을 옮기면 마지막 편집을 잃던 것.
- `[골대 원위치]` 가 [코트 비우기] 확인 창 안에 숨어 있던 것 — 인스펙터로 옮겼습니다.
- 모달 Esc 가 한글 조합 취소를 가로채던 것 · 다이얼로그가 열린 채 배경이 포커스를 가져가던 것.

### 알려진 제약

- 판 네 모서리의 둥근 부분(약 5 px)에서는 벤치로 되돌리기가 안 먹습니다.
- 좁은 세로 화면(폭 774 px 미만)에서 벤치 띠가 3줄이 되어 도구 줄이 처음에 안 보입니다.
- 거리 원 순환은 포인터 전용입니다 — 키보드로 3 m/5 m 원을 켤 방법이 아직 없습니다.
- 규칙 존은 화면·PNG 에만 있고 **인쇄물에는 안 실립니다**(0.1.0 과 같음).
- 새로고침하면 되돌리기 이력은 사라집니다(배치만 살아납니다). 줌·화면 이동도 100% 로 돌아옵니다.

## [0.1.0] 2026-08-13

두 번째 배포. <https://spin.atit.dev>

0.0.1 은 "파워체어 풋볼 전술 보드가 세상에 없어서" 만든 첫 삽이었습니다. 이번 배포는 그 판을 **초급·중급·고급 코치가 한 앱에서 같이 쓸 수 있게** 다시 짠 것입니다. 세 갈래로 갔습니다.

1. **판이 손에 잡히게** — 겹친 휠체어가 스스로 벌어지고, 놓은 자리가 그대로 있고, 빗나가도 잡힙니다. 메뉴를 아무리 줄여도 판이 손에 안 잡히면 앱을 닫습니다.
2. **밖으로 나가게** — 기기 이사 파일·PNG·인쇄(PDF). 판에서 만든 것이 종이와 단톡방까지 갑니다.
3. **규격이 맞게** — FIPFA Laws of the Game 2025 원문과 대조했습니다. 코트 3단·코너 인크로치먼트 마크가 들어오고, **규정에 없는 센터 서클이 빠졌습니다.**

### 추가됨

#### 내보내기

- **[기기 이사 파일]**: 예전 '전체 내보내기'는 드릴 목록만 담아서, **세션·설정·자유 전술판이 어떤 파일에도 안 들어갔습니다.** 사용자는 백업했다고 믿고 기기를 바꿨다가 잃습니다. 이제 한 파일에 전부 담기고, 복원하면 드릴의 만든 시각까지 보존됩니다. 가져오기는 **성공·건너뜀·손상 세 숫자를 전부** 알려 줍니다 — 열 개를 넣었는데 일곱 개만 들어왔다면 그 사실을 말해야 합니다.
- **PNG 내보내기**: 판 한 장면을 그림으로 뽑아 단톡방·밴드에 붙입니다. 글자(등번호·골키퍼 `G`·메모)는 SVG 텍스트가 아니라 **캔버스에서 그립니다** — 폰트가 없는 기기에서 열면 글자가 사라지거나 폭이 달라져 배치가 어긋나기 때문입니다.
- **인쇄 = PDF**: 브라우저의 "PDF 로 저장" 을 씁니다. PDF 라이브러리를 넣으면 한글 폰트를 통째로 임베드해야 해서 번들이 수 MB 불어납니다. **세션 계획서**(표지 + 드릴별 한 장)와 **드릴 시트**(스텝별 한 장) 두 가지가 나오고, 준비물 목록은 **드릴 전체 스텝의 합집합**으로 셉니다 — 현재 스텝만 보면 뒤에 등장하는 선수와 콘이 빠져 훈련장에 모자라게 들고 갑니다.
- **흑백 프린터·색각 이상 대책**: 팀 구분이 색 하나(빨강/파랑)에만 걸려 있어서, 흑백으로 뽑으면 **확정적으로** 두 팀이 구분 불가였습니다. 상대팀 칩에 파선 테두리를 더했습니다. 밝은 차체(주황·노랑·초록 골키퍼)에서는 테두리가 **검은색으로 뒤집힙니다** — 흰 테두리는 밝은 칩 위에서 대비 1.55:1 로 사실상 안 보였습니다.
- **iPad 공유**: `a[download]` 가 사실상 동작하지 않는 사파리에서는 시스템 공유 시트로 넘깁니다. 다운로드 URL 폐기도 미뤘습니다 — 같은 틱에 지우면 브라우저에 따라 **빈 파일이 받아집니다.**

#### 코트 규격 (FIPFA Laws 2025)

- **코트 크기 3단 — 30×18 m(최대) / 28×15 m / 25×14 m(최소)**: 체육관마다 실제 코트가 다른데 앱은 최대 규격 하나만 알았습니다. **28×15 는 표준 농구 코트 크기**라 국내에서 가장 흔합니다. 드릴은 자기가 어느 크기였는지 기억하고, 기존 드릴은 전부 30×18 로 열립니다. 크기 전환은 **판이 비어 있을 때만** 열립니다 — 좁은 코트로 옮기면 선수가 밖에 서기 때문입니다.
- **코너킥 인크로치먼트 마크**(2025 신설): **각 골포스트 안쪽 1 m 지점에서 골라인에 수직으로, 필드 밖 방향**으로 긋습니다. 코너에서 재는 거리가 아닙니다 — 코트 크기를 바꾸면 코너에서 마크까지의 거리가 달라지는 것이 그 증거입니다.
- **센터 마크(15 cm X)**: 하프라인 한가운데. 하프 코트에는 하프라인이 없으므로 넣지 않습니다.
- **세트피스 시작 배치**: 킥인·코너킥·골 클리어런스 배치를 버튼 하나로 세웁니다. 이격 거리는 상황마다 기준점이 다릅니다 — 킥인은 공, 코너킥은 코너 삼각형. "5 m 원 하나" 로 뭉치면 틀립니다.
- **공을 따라다니는 3 m 링과 골 지역 3인 판정**.

#### 판 다루기

- **2단 히트**: 빗나가도 44 CSS px 안에 개체가 있으면 잡힙니다. 다만 **지우개에는 걸지 않았습니다** — 걸면 지우개가 44 px 반경의 파괴 도구가 됩니다.
- **화살표를 키보드로 옮깁니다**: 방향키로 전체 이동, Shift+방향키로 끝점 이동. 전술 드릴에서 화살표는 '누가 어디로' 의 본체인데, 그전까지 유일한 경로가 12 px 이상 드래그 + 반경 22 px 핸들 세 개의 정밀 드래그였습니다 — 발 마우스·입 젓가락 사용자에게는 사실상 접근 불가였습니다.
- **`Esc` 로 선택 해제, 선택된 개체를 다시 탭해도 해제**: 코트가 붐비면 "빈 곳" 이 없어서 선택을 풀 방법이 사라졌습니다.
- **잡으면 칩이 판에서 뜹니다**: 그림자가 깊어져 무엇을 쥐고 있는지 눈으로 보입니다.
- **판 테두리를 잡으면 판이 움직입니다** + `Ctrl+방향키` 로도 밉니다.
- **스텝을 판 사진 뭉치로 넘깁니다**: 하단 스크러버에서 끌어서 순서를 바꿀 수 있고 키보드로도 됩니다. 스텝을 넘길 때 화살표·메모까지 함께 트윈하고 등장/퇴장은 페이드합니다.
- **2존 모드**(접근성 설정, 기본 꺼짐): 켜면 차체 전체가 하나로 움직입니다. 잡는 영역이 하나면 실수로 앞쪽을 잡아 방향이 틀어지는 일이 없습니다.
- **놓으면 소리가 나고 손끝이 울립니다**: WebAudio 로 합성하므로 음원 파일이 없습니다. 설정에서 끄면 오디오 장치를 **아예 열지 않습니다.**

#### 드릴을 가르치는 물건으로

- **교육 필드**: 목적·코칭 포인트·필요 인원·장비, 그리고 반복·세트·인터벌. 고급 코치가 만든 드릴을 초보 코치가 열었을 때 *무엇을 왜 하는지* 가 판에만 있으면 전달되지 않습니다.
- **선수 실명**: 등번호 대신 이름으로 부릅니다. 트레이와 시연 자막이 읽습니다.
- **스텝 이름·메모·시간**을 인스펙터에서 적습니다.
- **드릴 카드에서 [시연] 1클릭 직행**.
- **고대비·강제색 대응**: `prefers-contrast` 와 Windows 고대비(`forced-colors`)에서 코트·팀 색·규칙 존이 시스템 색으로 바뀌어도 판이 읽힙니다. 그전까지 저장소 전체의 `@media` 는 `prefers-reduced-motion` 하나뿐이었습니다 — 접근성 축에서 **저시력 사용자가 통째로 빠져 있었습니다.**
- **도움말 첫 섹션이 '놓기·옮기기'** 로 바뀌고 화면에서 바로 열립니다.

### 변경됨

- **메뉴가 3단이 되었습니다 — [보드] · [드릴] · [설정]**: 다섯 화면을 셋으로 접었습니다. 시연은 화면이 아니라 드릴에서 가는 곳이라 레일에서 내렸습니다(뒤로가기는 그대로 동작합니다). 좁은 창에서는 왼쪽 레일이 **헤더 좌측 3칸 세그먼트로 접혀** 코트에 84 px 을 돌려줍니다.
- **인스펙터가 판을 밀지 않고 판 위에 뜹니다**: 예전에는 인스펙터를 열면 코트 상자가 좁아져 판이 통째로 작아졌습니다. 이제 오버레이 시트이고, PC(폭 1100 이상)에서는 [고정] 핀으로 붙박이로 만들 수 있습니다.
- **첫 화면 컨트롤을 40개 이하로 묶었습니다**: 인스펙터 20개가 오버레이로 내려간 것이 절감의 절반입니다. 상한을 넘기면 테스트가 빨간불이 되므로, 앞으로 버튼을 더하려면 무언가를 빼야 합니다.
- **트레이가 서랍 둘로 갈렸습니다** — 작도와 설명. 이 드릴이 **실제로 쓰는** 것만 열립니다(3번 스텝에만 화살표가 있어도 그 드릴은 화살표를 쓰는 드릴입니다). 개폐는 기억됩니다.
- **설정에서 물리 6종이 닫힌 서랍으로 들어갔습니다**: 대부분의 코치가 평생 안 만지는 값이 정작 필요한 접근성 설정을 아래로 밀어냈습니다. 소리·햅틱·2존·고대비는 접근성 절로 모았습니다.
- **센터 서클이 사라졌습니다**: Laws 2025 전문 50쪽에 *"circle"* 이 **한 번도 나오지 않습니다.** 일반 축구 감각이 흘러든 선이었습니다.
- **내보내기 진입점이 하나가 되었습니다**: 설정과 목록에 흩어져 있던 '전체 내보내기' 를 없애고, [보드] 하단 [내보내기] → 3항목 시트로 모았습니다.
- **대문 대시보드를 걷어냈습니다**: '다음 세션' 스트립만 세션 탭 머리로 옮겼습니다. 세션이 하나도 없으면 목록이 드릴 탭으로 열립니다.
- **터치 타깃을 키우면 트레이가 93 → 117 px 로 함께 넓어집니다**: 버튼만 키우고 담는 곳을 안 키우면 칩이 서로 겹칩니다.

### 고쳐짐

- **겹친 휠체어가 스스로 벌어집니다**(0.0.1 알려진 이슈): 손을 뗀 뒤 겹침이 남아 있는 동안 물리를 계속 돌립니다. 최대 12초 안에 반드시 멈추고, 시한을 넘기면 기하적으로 한 번 떼어 놓습니다.
- **놓은 자리가 그대로 있습니다**: 그전에는 손을 뗀 좌표를 저장하고 물리는 그 뒤로도 계속 굴러서, 스텝을 넘겼다 돌아오면 칩이 **손 떼던 자리로 되돌아갔습니다.** 이제 정착이 끝난 좌표로 다시 커밋하고 그때 격자·코트 라인·이웃 축·포메이션 슬롯·세트피스 스팟에 붙입니다. 되돌리기 횟수는 늘지 않습니다(드래그 한 번 = 되돌리기 한 번).
- **밀려난 휠체어가 판 밖에 굳지 않습니다**.
- **편집 중 판이 얼어붙던 두 경로**(0.0.1 알려진 이슈): 드래그 도중 정착이 시작되던 것과 rAF 체인이 갈라지던 것. 원인을 특정해 고쳤지만 **보고된 그 멈춤이라는 증명은 없어서 이슈를 닫지 않았습니다.**
- **메모가 탭한 자리에 종이 쪽지로 나타납니다**: 그전에는 메모 도구로 탭해도 아무 일도 일어나지 않는 것처럼 보였습니다.
- **트레이 칩을 키보드로 활성화할 수 있습니다**: 포커스 후 `Enter` 가 동작하지 않았고, 마우스에서는 반대로 두 번 발화하고 있었습니다.
- **세로 화면에서 서랍을 열면 앞쪽 버튼이 밀려나던 문제**: 가로 트레이에서만 발생해 오래 숨어 있었습니다. 버튼의 절대 위치로 공간 기억을 만드는 사용자에게 서랍 개폐마다 표적이 85 px 씩 이동했습니다. 같은 원인으로 **7인치 세로 화면에서는 1번 선수를 영영 잡을 수 없었습니다**(넘친 앞쪽을 스크롤로 끌어올 수 없음).
- **앱이 자기가 만든 백업 파일을 "형식이 이상하다" 며 거절하던 문제**.
- **설정의 물리 존 슬라이더가 실제 드래그 판정에 전혀 반영되지 않던 문제**: 음영과 커서는 설정을 따라 움직여서, **"제자리 회전" 이라 칠해진 곳을 잡으면 평행 이동이 되었습니다.**
- **설정의 '마지막 스텝에서 반복' 이 재생에 한 번도 닿지 않던 문제**: 켜도 그냥 멈췄고, 시연 화면에서 [반복]을 드릴마다 다시 눌러야 했습니다.
- **시연 화면이 Windows 고대비에서 팀 구분을 잃던 문제**: 편집기와 인쇄물은 멀쩡한데 **선수에게 보여 주는 화면만** 뭉개졌습니다.
- **시연 진행 막대가 고대비에서 "어디까지 했는가" 를 잃던 문제**, **고대비에서 주 버튼이 배경색으로 뭉개지던 문제**.
- **설정 설명문 2건이 거짓이었던 것**: '기본 코트 모드' 가 은퇴한 화면을 가리키고 있었습니다.

<!-- ↓ 아래 세 절은 0.0.1 직후 · 재편 착수 이전에 쌓여 있던 것으로, 이번 0.1.0 에 함께 나갑니다 -->

### 변경됨 — 트레이와 칩 (재편 이전)

- **트레이가 도구 모음에서 개체 보관함이 되었습니다**: 선수는 **주차 슬롯**, 공과 콘은 **상자**입니다. 코트로 끌어다 놓으면 꺼내지고, 코트에서 트레이로 도로 끌어다 놓으면 제자리로 돌아갑니다. 지우개로 지우는 것과 같은 동작이지만, 손이 이미 개체를 쥐고 있을 때 도구를 바꾸러 갔다 오지 않아도 됩니다. 되돌리기 한 번으로 살아납니다.
  - 코트에 나가 있는 선수는 트레이에서 **빈 점선 자리**로 남습니다. 자리가 사라지면 배치할 때마다 트레이 길이가 출렁이고, 무엇보다 코트에서 빼낸 말이 어디로 돌아가는지가 보이지 않습니다.
  - 상자에는 **남은 개수**를 표시합니다. 놓은 개수가 아닙니다 — 손이 다음에 알고 싶은 것은 "몇 개 놓았나" 가 아니라 "몇 개 더 꺼낼 수 있나" 입니다. 개수는 이름이 아니라 설명으로 읽히므로, 개수가 바뀌어도 스크린리더에서 같은 버튼이 다른 것으로 들리지 않습니다.
- **콘이 색깔마다 상자를 따로 갖습니다**: 예전에는 콘 버튼을 다시 눌러 색을 바꾸는 팝오버였는데, 색마다 남은 개수가 따로 있으면 배지 하나로 둘을 나타낼 수 없습니다. 상자를 나누면 어느 색이 몇 개 남았는지가 누르기 전에 보입니다. 끌어다 놓을 때는 **끌고 있는 상자의 색**이 이깁니다.
- **콘 색이 분홍에서 파랑으로 바뀌었습니다**: 주황과 분홍은 적록색맹 시뮬레이션에서 서로 가까워, 두 색 콘을 구분하는 훈련에서 쓸 수 없었습니다.
- **공은 최대 10개에서 8개로, 콘은 색깔마다 8개씩입니다**.
- **공과 콘에는 속도 제한이 걸리지 않습니다**: 10 km/h 는 파워체어의 속도입니다. 공과 콘은 코치의 손이 판 위에서 옮기는 말이지 주행하는 물건이 아니라, 같은 상한을 걸면 판을 짜는 내내 손을 기다리게 됩니다. 하단 스위치는 이제 휠체어에만 적용됩니다.
- **풀 코트에서도 선수를 처음 놓으면 세로로 섭니다**: 골대가 좌우에 있어 공격 축을 따르면 가로(0°/180°)가 맞지만, 판을 짤 때 먼저 필요한 것은 "지금 어디를 보고 있는가" 가 아니라 "누가 어디에 있는가" 입니다 — 방향은 그 다음에 돌려 잡습니다. 이제 세 코트가 모두 같은 값이라, 코트를 바꿔도 말이 서 있는 모습이 달라지지 않습니다. 두 팀은 여전히 서로 마주 봅니다. 트레이에서 끌 때 커서에 매달리는 칩도 **놓일 각도 그대로** 매달립니다 — 눕혀 두면 손을 떼는 순간 칩이 홱 돌아 내려놓은 자리와 눈이 어긋납니다(크기를 코트 축척에 맞추는 것과 같은 이유입니다).
- **트레이 칩이 코트 칩의 축소판이 되었습니다**: 예전 트레이 칩은 그냥 둥근 사각형이라 머리와 볼가드가 어느 쪽인지 알 수 없었습니다. 이제 코트와 **같은 좌표계**로 그려서 비율·머리·볼가드·등번호 위치가 전부 같습니다 — 차체 치수를 바꾸면 트레이가 따라옵니다. 방향은 **앞이 위를 향한 세로**입니다: 트레이가 세로 레일이라 눕히면 한 줄에 두 개가 못 들어가고, 선수 8명이면 트레이가 두 배로 길어집니다. 등번호는 코트에서와 마찬가지로 절대 눕지 않습니다.
- **차체를 잡는 곳이 둘로 줄고, 경계가 한가운데로 왔습니다**: 예전에는 차체를 네 토막으로 나눠 뒤쪽·머리·회전·앞범퍼를 각각 다르게 잡았는데, 등번호 근처를 잡았는데도 휠체어가 끌려가는 일이 있었습니다. 이제 차체는 **뒤 절반이 그대로 이동, 앞 절반이 제자리 회전** 둘뿐이고, 견인(줄로 끌기)은 차체 **밖**으로 뻗은 앞뒤 가이드 핸들 전용입니다. 두 구역은 음영 농도로 구분됩니다 — 터치에는 커서가 없어서 태블릿에서는 눈으로 보이는 것이 유일한 단서입니다. (처음 재편에서는 뒤 1/3 : 앞 2/3 이었고, 같은 날 반반으로 옮겼습니다.)
- **칩의 등번호와 골키퍼 `G` 를 2/3 크기로 줄였습니다**: 칩 위에 존 음영·선택 링·가이드가 겹치면서 글자가 차체를 꽉 채워 답답했습니다.
- **선택했을 때 뜨는 가이드를 앞뒤 바깥 둘로 줄였습니다**: 차체 안쪽에 있던 가이드 두 개를 없앴습니다. 차체 자체가 곧 두 구역이고 그것은 음영과 마우스 커서로 이미 표시되는데, 같은 자리에 가이드까지 얹으면 등번호를 가리고 무엇을 잡아야 하는지가 오히려 흐려졌습니다. 견인은 차체 밖에서만 잡히므로 앞뒤 가이드는 남깁니다.
- **도구와 개체를 판 오른쪽 트레이로 모았습니다**: 선수·공·콘을 트레이에서 코트로 끌어다 놓을 수 있습니다. 탭으로 놓는 경로와 끌어다 놓는 경로가 같은 배치 함수를 쓰므로, "탭으로는 10개에서 막히는데 드래그로는 11개째가 놓인다" 같은 어긋남이 생기지 않습니다.

### 추가됨 — 판 이동과 속도 스위치 (재편 이전)

- **판 이동(더블클릭 후 끌기)**: 확대는 되는데 이동이 없어서, 200% 에서 코트 왼쪽 절반은 볼 방법이 없었습니다. 코트를 더블클릭하면 커서가 십자가로 바뀌고, 그 상태로 끌면 판이 밀립니다. 더블클릭한 손가락을 떼지 않고 그대로 끌어도 되고(터치에는 커서가 없어 이 쪽이 자연스럽습니다), 떼었다가 따로 끌어도 됩니다. `Esc` 로 풉니다.
  - 한 손가락 끌기는 고무줄 선택이, 두 손가락은 핀치 확대가 이미 쓰고 있어 남은 손짓 중에서 골랐습니다. 모드 버튼을 하나 더 만들지 않아도 되고, 잘못 눌러도 손을 떼면 끝납니다.
  - **선택 도구에서만** 무장합니다. 배치 도구에서는 같은 자리에 콘 두 개를 빨리 찍는 것이 더블클릭으로 읽혀 두 번째가 삼켜집니다.
  - **고무줄 선택 중에는 가장자리에서 판이 저절로 따라옵니다**: 확대해 놓고 선택을 시작하면 화면 밖 개체는 어떤 방법으로도 사각형 안에 넣을 수 없었습니다 — 손을 떼면 선택이 끝나고, 떼지 않으면 판을 밀 수 없기 때문입니다. 이제 포인터가 가장자리 56px 안으로 들어가면 그 방향으로 판이 밀립니다. 속도는 가장자리에 닿는 순간 0에서 시작해 제곱으로 붙습니다 — 선형이면 띠에 들어서는 순간 판이 홱 튀어 조준을 잃습니다. 개체를 잡은 드래그에는 걸지 않았습니다(잡은 개체가 화면 밖으로 딸려 나갑니다).
  - 판이 갈 수 있는 범위는 확대와 **같은** 클램프를 씁니다 — 두 곳에서 따로 자르면 확대해 놓고 밀었을 때만 판 밖으로 나갑니다.
- **개체 이동 속도 제한 스위치**: 판 하단에서 껐다 켤 수 있습니다. 켜면 실제 파워체어 속도(10 km/h)로 움직이고, 끄면 잡은 개체가 포인터를 즉시 따라옵니다. 실제 속도로 움직임을 확인하는 것과 판을 빨리 짜는 것은 목적이 다르고 한 세션 안에서 수시로 오가므로, 설정 화면이 아니라 판 위에 뒀습니다. 껐을 때를 액센트로 강조합니다 — 제한을 푼 채로 두고 왜 빠른지 모르는 상황이 더 나쁩니다.
- **왼쪽 레일 하단에 버전 표시**: 사용자가 "지금 뭘 보고 있는지" 말할 수 있어야 제보를 특정 빌드에 붙일 수 있습니다. 값은 `package.json` 하나에서 나오며(빌드 시점 주입), 화면 문자열과 `package.json`이 어긋나면 테스트가 빨간불이 됩니다.

### 고쳐짐 — 재편 이전

- **지운 공·콘이 유령으로 남아 배치 상한을 막던 문제**: 삭제가 배치 좌표만 지우고 명단은 그대로 둬서, 화면에 보이지 않는 개체가 "공은 최대 10개" 상한을 영원히 물고 있었습니다. 사용자 제보로 발견했습니다.
- **설정의 존 경계 기본값이 저장 단계에서 잘려 나가던 문제**: 경계값을 보정하는 클램프의 상한이 기본값보다 낮아서, 아무도 설정을 건드리지 않았는데도 실제 적용값이 기본값과 달라졌습니다. 기본값이 클램프에 잘리지 않는지 확인하는 회귀 테스트를 함께 넣었습니다.

### 알려진 이슈

- **선수 이름이 종이와 PNG 에는 나오지 않습니다** — 화면과 시연 자막에만 흐릅니다.
- **세션 계획서의 메모·휴식 열이 빈칸**입니다.
- **드릴을 지울 때 '세션에서 사용 중' 경고가 없습니다.** 확인창도 없고, 토스트가 사라지면 되돌릴 길이 없습니다.
- **예제 드릴의 내용이 아직 비어 있습니다** — 심는 장치는 완성됐고 파일 하나만 채우면 됩니다.
- **실기로 확인하지 못한 것**: iPad 공유 · 인쇄 물리 장 수 · 흑백 하프톤에서 파선이 살아남는지 · 한글 글리프 래스터화 · **7인치 태블릿 세로 배치**. 태블릿 실물이 없어 완료 판정의 일부가 종이 위에 남아 있습니다.
- **편집 중 브라우저가 멈추는 현상**(0.0.1)은 두 경로를 특정해 고쳤으나, *보고된 그 멈춤이었다* 는 증명이 없어 **이슈를 닫지 않습니다.**

## [0.0.1] 2026-08-10

첫 공개 배포. <https://spin.atit.dev> 에 올라간 빌드입니다.

파워체어 풋볼(전동휠체어 축구) 전용 전술 보드는 세상에 없어서, 4v4 코트 위의 움직임을 스텝 단위로 설계하고 팀 앞에서 그대로 재생하는 것을 목표로 만들었습니다.

### 추가됨

- **자유 전술판**: 대문이 곧 전술판입니다. 풀·하프·플랫 코트를 자유롭게 바꿔가며 그리고, 마음에 들면 정식 드릴로 저장합니다. 전술판과 드릴 편집기는 같은 컴포넌트를 씁니다. 코트 형태 전환은 **판이 비어 있을 때만** 열립니다 — 30×18 m 와 18×15 m 는 어떤 방법으로도 같은 전술이 될 수 없어서, 경고를 띄우고 배치를 날리는 대신 잃을 것이 없을 때로 전환을 한정했습니다.
- **휠체어 드래그 운동학**: 회전축이 차체 뒤에서 전체 길이의 1/5 지점(탑승자 머리)에 있습니다. 잡은 위치에 따라 후방 견인·평행 이동·제자리 회전·전방 견인으로 갈리며, 견인은 줄을 매달아 끄는 것처럼 차체 방향이 자연스럽게 돌아갑니다. 속도는 전후진 10 km/h, 회전은 앞범퍼 기준 30 km/h(약 398°/s, 한 바퀴 0.9초)이고 설정에서 조정할 수 있습니다.
- **드릴 편집기**: 선수·공·콘·화살표·메모를 배치하고 스텝 단위로 동작을 설계합니다. 공은 최대 10개, 콘은 2가지 색입니다. 되돌리기·다시하기, 키보드만으로 하는 개체 이동·회전을 지원합니다.
- **격자 오버레이**: 풀 코트는 가로 6 × 세로 5 칸(5 m × 3.6 m), 하프 코트는 90° 회전 기준 가로 5 × 세로 3 칸, 플랫 코트는 1 m 간격입니다. 칸 번호는 `a1`·`b4` 형식으로 흐릿하게 표시하되, 플랫 코트만 스프레드시트식 축 헤더를 씁니다 — 1 m 격자는 340칸이라 칸마다 얹으면 판독 불가한 노이즈가 됩니다.
- **시연 모드**: 전체화면, 화면 꺼짐 방지, 스와이프로 스텝 이동. 훈련 세션으로 드릴 여러 개를 순서대로 묶어 총 시간을 계산하고 연속으로 시연합니다.
- **로컬 저장**: 서버가 없습니다. 드릴은 브라우저에 저장되고 하나씩 `.json` 파일로 내보내거나 가져올 수 있습니다. 체육관에서 인터넷 없이 씁니다.
- **태블릿 대응**: 코트의 긴 축을 화면의 긴 축에 맞춰 판을 회전시키고, 앱 셸을 창 크기에 가둡니다. "홈 화면에 추가"하면 브라우저 크롬 없이 전체화면으로 뜹니다(PWA).
- **골대 물리**: 골대가 물리 바디입니다. 휠체어에는 밀리지만 공에는 밀리지 않습니다. 밀려난 골대를 되돌리는 `골대 원위치` 버튼이 있습니다.
- **접근성**: 키보드만으로 코트 위 개체를 옮기고 돌릴 수 있습니다. 포커스 표시, 터치 타깃 크기, 색 대비를 WCAG 기준으로 맞췄고, 단일 문자 단축키는 설정에서 끌 수 있습니다.

### 변경됨

- **휠체어끼리 묵직하게 밀립니다**('push' 모드 1단계). 이때 잡고 있는 칩만 고정체로 두는데, 전부 움직이는 물체로 두면 운동량을 전달하지 못하고 반동으로 밀려나 공 밀기 속도가 2.78 → 0.64 m/s 로 죽습니다.
- **공·콘 질량을 실물값으로** 맞추고 구름 감쇠를 재조정했습니다. 평면 물리에는 중력도 구름 감속도 없어서, 감쇠를 주지 않으면 한 번 밀린 물체가 영원히 미끄러집니다. "묵직하다"의 절반은 질량이 아니라 감쇠였습니다.
- **코트 외곽 마진을 1.0 → 1.5 m** 로 넓혀 골대와 라인 밖 개체가 판 가장자리에 끼지 않게 했습니다.

### 고쳐짐

- **개체를 잡는 순간 이전 자리로 튀던 문제 2건**: 스텝을 넘기거나 키보드로 옮겼을 때 화면만 갱신되고 물리 상태가 따라오지 않아, 그 뒤에 마우스로 잡으면 옛 자리로 되돌아갔습니다. 화면은 멀쩡해 보여서 잡기 전까지는 드러나지 않는 종류였습니다.
- **드래그 존이 칩과 따로 놀던 문제**: 4개 드래그 핸들이 선택한 순간의 좌표에 멈춰 있어 칩을 끌어도 따라오지 않았습니다.
- **차체 밖 앞뒤 핸들이 눌러도 반응하지 않던 문제**: 화면에는 그려지는데 히트 판정 조건이 달라서, 누르면 빈 코트로 떨어져 선택만 해제됐습니다.
- **드래그 연결선이 잡은 지점이 아니라 회전축에서 나가던 문제**: 어느 곳을 잡든 선이 늘 차체 한가운데에 붙어서, 조작 자체가 축을 끄는 것처럼 읽혔습니다.
- **비스듬히 끌면 차체가 돌지 않던 문제**: 줄이 느슨해졌는지를 각도로 판정하고 있어서, 옆으로 비스듬히 끄는 정상적인 동작까지 회전이 죽었습니다(135° 방향으로 1초를 끌어도 제자리). 판정을 "목표점이 줄 길이 안쪽인가"로 바꿔, 실제로 미는 동작만 걸러냅니다.
- **세션에서 다음 드릴로 넘어갈 때 2초 동안 이전 배치가 남던 문제**: 안내 문구가 "다음 드릴"을 알리는 내내 코트는 이전 드릴을 보여줘, 체육관에서 보면 전환이 실패한 것처럼 보였습니다.
- **세로 화면에서 회전한 코트가 아래 도구를 화면 밖으로 밀어내던 문제**.
- **코트 외곽선이 마진 변경을 따라오지 않아** 골대가 선 밖으로 나가던 문제.
- **밀려난 골대를 되돌릴 때 휠체어에 막히면 조용히 실패하던 문제**.

### 알려진 이슈

- 겹친 휠체어가 서로 밀려나지 않는 경우가 있습니다.
- 편집 중 브라우저가 멈추는 현상이 보고됐고 원인을 특정하지 못했습니다.
- 이미지·PDF 내보내기가 아직 없습니다.

<!-- Links -->

[keep a changelog (korean)]: https://keepachangelog.com/ko/1.0.0/
[semantic versioning (korean)]: https://semver.org/lang/ko/
