// 로더 마크의 색 — 정본은 `public/logo.svg` 다 (PLAN-0-6-3-LOADER-NOTICE 결정 32).
//
// 왜 `styles/tokens.css` 에 넣지 않는가: 토큰은 **테마마다 갈리는 값**의 자리다. 이 셋은
// 브랜드 색이라 라이트·다크에서 같고(레일 로고가 이미 그렇다), 토큰으로 올리면 "다크에서
// 초록을 낮춰야 하나" 라는 질문이 없는 곳에 생긴다. 그래서 색 리터럴을 쓰는 이 저장소의
// 예외 하나이고, 대신 **한 파일에 모아** 흩어지지 않게 한다.
//
// ⚠️ 아이콘 색을 바꿀 때는 `public/logo.svg` 를 먼저 고치고 여기가 따른다(AGENTS §3 정본 우선).
// 파비콘·PWA 아이콘·레일 로고가 전부 그 파일에서 구워지므로, 여기만 고치면 로더 마크 혼자
// 다른 색이 된다. 그림이 무엇인지도 그 파일 주석이 정본이다 — 원 두 겹이 **코트 바닥**,
// 45° 눕힌 둥근 사각이 **휠체어**, 흰 원이 **머리**다.
//
// 공(`BALL_FILL`)만 재수출인 이유: 공은 브랜드 색이 아니라 **앱이 코트에 그리는 그 공**이라
// `core/colors.ts` 가 정본이고, 로고가 그 값을 따라간 쪽이다.
export { BALL_FILL } from '../../core/colors.ts';

/** 코트 바깥 테(어두운 초록). 공의 테두리도 같은 색이다 — 밝은 초록 위에서 형태가 또렷하다. */
export const MARK_COURT_RIM = '#2f5d2b';

/** 코트 바닥(밝은 초록). */
export const MARK_COURT_FILL = '#a4e70c';

/** 휠체어 차체(파랑). */
export const MARK_CHAIR = '#2e6db6';

/** 차체 뒤끝 파선과 머리. 흰색도 마크의 일부라 여기 둔다 — 리터럴이 컴포넌트로 새지 않게. */
export const MARK_WHITE = '#fff';
