# SPIN 로드맵

> **이 문서가 "앞으로 할 일" 의 정본입니다.** `docs/PLAN-2026-08.md` 는 2026-08 재편의
> **역사 기록으로 동결**했습니다 — 거기 §7·§9 의 살아 있던 행은 전부 이 문서로 옮겼습니다.
> 앞으로 할 일을 두 곳에 적으면 두 곳이 갈라지고, 갈라지면 둘 다 못 믿게 됩니다.
>
> **다만 `docs/PLAN-2026-08.md` §8 「하지 않을 것」 20여 행은 그 문서에 그대로 둡니다** —
> 근거가 붙은 *기각 기록*이라 성격이 다릅니다. **새 기능을 제안하기 전에 그 절을 먼저
> 읽으십시오.** 이미 근거까지 붙여 기각한 것을 서너 달마다 다시 검토하는 것이 이 저장소가
> 겪은 낭비입니다.

**갱신**: 릴리스마다. 버전을 올릴 때 그 절을 닫고 다음 절을 엽니다.
**현재 버전**: 0.3.0 (2026-08-19)

---

## 큰 그림

| 버전 | 성격 |
|---|---|
| **0.3** | 현장 피드백 — 코트에서 걸리는 것을 계속 고칩니다 |
| **0.4** | 미배송 빚 — *"저장은 되는데 화면엔 없다"* 를 닫습니다 |
| **0.5** | Tauri 데스크톱 — 리눅스·윈도우·맥 |
| **0.6** | 구글 드라이브 동기화 — 기기 사이를 파일 없이 오갑니다 |
| **0.7** | iOS · Android |
| **1.0** | 전 플랫폼 배송 + 실기 매트릭스 통과 |
| **2.0** | 포털 — 드릴·세션 커뮤니티 |

### ⚠️ "서버 없음" 원칙은 단계적으로 풉니다

`docs/REQUIREMENTS.md` §1 과 README 가 **"로컬 전용, 서버 없음"** 을 요구로 못박고 있습니다.
그 전제는 아래 순서로 바뀝니다. **바꾸는 시점에 그 두 문서도 함께 고칩니다.**

| 단계 | 상태 |
|---|---|
| ~0.5 | **그대로.** 앱은 서버를 안 씁니다. 기기 이동은 JSON 파일 하나입니다 |
| 0.6 | **내 계정 동기화.** 브라우저가 **사용자의** 구글 드라이브에 직접 씁니다 — 내 서버도 내 DB 도 없고, 남의 드릴을 내가 보관하지 않습니다. 로컬이 여전히 원본입니다 |
| 2.0 | **진짜 서버.** 커뮤니티에는 계정·검색·신고가 필요하고, 그건 서버 없이는 안 됩니다 |

---

## 0.3 — 현장 피드백

지금까지와 같은 방식입니다. 코트에서 써 보고 걸리는 것을 고칩니다. **이 절은 미리 다 채우지
않습니다** — 실기에서 나오는 것이 들어옵니다. 아래는 지금 알고 있는 것입니다.

- 판 네 모서리의 둥근 부분(약 5 px)에서 벤치로 되돌리기가 안 먹는 것
- 좁은 세로 화면(폭 774 px 미만)에서 벤치 띠가 3줄이 되어 도구 줄이 처음에 안 보이는 것
- 거리 원(3 m/5 m) 순환에 **키보드 경로가 없는 것** — 지금은 포인터 전용입니다
- 규칙 존(골 지역 표시)이 **인쇄물에 안 실리는 것**
- 새로고침하면 되돌리기 이력이 사라지는 것 · 줌·화면 이동이 100% 로 돌아오는 것
- 0.2.0 에서 바꾼 것들의 실기 확인 — 메모 모달이 코트를 가리는 정도, 태블릿에서 더블탭이
  실제로 열리는지, 진영 깃발이 멀리서 읽히는지, 트레이 드롭 예고가 체육관에서 보이는지

## 0.4 — 미배송 빚

*"저장은 되는데 화면엔 없다"* 는 이 저장소의 고유 함정입니다. 값이 저장·마이그레이션·검증·
백업까지 왕복하는데 화면에 한 픽셀도 없는 상태입니다. 아래는 그 전수 색출이 찾은 목록이고,
**0.2.0 시점에도 거의 그대로 남아 있습니다.**

- **팀 이름·골키퍼 색 입력** — `teams[side].label`·`.gkColor` 를 바꿀 UI 가 0곳인데
  렌더·스크린리더·라이브 리전 셋이 그 값을 읽습니다
- **세션 메모 · 항목 메모 · 휴식 시간 입력** — 세 필드 모두 검증을 통과하는데 입력 자리가
  없어 **인쇄된 훈련 계획서의 세 열이 영구히 빈칸**입니다
- **드릴 삭제 앞 '세션에서 사용 중' 경고** — `findReferrers` 에 호출자가 없습니다
- **저장소 열화·압박 경고 배너** — 설계는 있고 소비처가 0곳입니다
- **메모 크기·색 입력** — 모델에 `size`·색이 있는데 고칠 자리가 없습니다
- **선수 실명을 종이·PNG 에 태우기** — 화면에만 있고 인쇄물에는 안 나옵니다
- **`propagateForward` 에 부를 자리 주기** — "이 스텝의 자세를 이후 전부에 밀어넣기" 가
  구현돼 있는데 부를 버튼이 없습니다
- **공·콘·메모 테두리를 `strokeFor` 로 넓히기** — 칩만 뒤집혀 있고 나머지는 흰색 고정입니다
- **죽은 export 청소** — `cubicBezier`·`pxToM`·`upcomingSession`·`CourtPreview` 등
- **`PresentRunner` 의 `effectiveReduceMotion` 중복 구현 제거** — 판정식이 두 벌입니다

> ⚠️ **첫 화면 표적 예산 ≤ 40 이고 지금 여유가 0 입니다**(초기 37 / 서랍 둘 다 열림 40).
> 위에서 컨트롤을 더하는 항목은 **반드시** 인스펙터·시트·모달·서랍 안으로 들어가야 합니다.
> `src/test/boardTargetBudget.test.tsx` 가 게이트입니다.

## 0.5 — Tauri 데스크톱

리눅스·윈도우·맥. 데스크톱을 먼저 하는 이유는 **심사도 스토어 계정도 서명도 없어서** 입니다 —
패키징·자동 업데이트·파일 접근을 여기서 익히고 모바일로 갑니다.

- Tauri 셸 + 빌드 파이프라인 (3 OS)
- 파일 열기/저장을 **네이티브 대화상자**로 — 브라우저 다운로드 폴더를 거치지 않습니다
- 자동 업데이트
- 창 크기·전체화면 상태 기억
- 앱 아이콘·설치 패키지(deb·AppImage·msi·dmg)
- 웹앱과 **같은 코드 한 벌** 유지 — 분기는 최소로

## 0.6 — 구글 드라이브 동기화

기기 사이를 **파일을 들고 다니지 않고** 오갑니다. 사용자의 드라이브에 직접 씁니다.

- 구글 계정 연결 (앱 전용 폴더 권한만)
- 드릴·세션·선수 명단 업로드/다운로드 — **설정은 제외**(2026-08-20 기현님 승인). 설정에는
  수정시각이 없어 최신 승 판정이 불가능하고, 통째로 덮으면 큰글씨·UI 배율 같은 접근성
  설정이 조용히 꺼지는 사고가 됩니다(백업 복원의 기본값이 같은 이유로 'skip' — transfer.ts).
  테마·배율은 기기별이 자연스럽기도 합니다(회사 모니터와 태블릿은 다릅니다)
- 충돌 처리 — 같은 드릴을 두 기기에서 고쳤을 때 무엇이 이기는가
- 오프라인 우선 — 체육관에 인터넷이 없어도 **전부 그대로 동작**해야 합니다. 동기화는 덤입니다
- 연결을 끊어도 로컬 데이터는 남습니다

## 0.7 — iOS · Android

- 모바일 셸(Tauri Mobile 또는 대안 — 0.5 결과를 보고 정합니다)
- 스토어 계정·서명·심사 대응
- 태블릿 가로/세로 실기 재검증
- 화면 꺼짐 방지·전체화면이 네이티브에서도 되는지 확인
- 파일 공유(iOS 공유 시트 · Android 인텐트)

## 1.0 — 전 플랫폼 배송

- 웹·데스크톱 3종·모바일 2종이 **같은 버전으로** 나갑니다
- `docs/FIELD-TEST.md` 기기 매트릭스를 실제로 채웁니다 (지금은 비어 있습니다)
- 씨앗 드릴 — 빈 앱으로 시작하는 사람이 처음 볼 것
- 사용 설명 — 앱 안에서 읽는 짧은 안내

## 2.0 — 포털

드릴과 세션을 전 세계 코치가 주고받는 곳. **여기서 "서버 없음" 이 끝납니다.**

### 무엇이 참이어야 시작하는가 (전제조건)

이 셋이 안 되면 포털은 빈 서버가 됩니다. **하나라도 아니면 시작하지 않습니다.**

1. **1.0 이 실제로 여러 기기에서 돌고 있다** — 만들 것이 아니라 쓰고 있는 앱이어야 합니다
2. **내 팀 밖의 코치가 SPIN 으로 드릴을 만들고 있다** — 공유할 것이 없으면 공유할 곳도 필요 없습니다
3. **운영을 감당할 수 있다** — 서버 비용, 신고 처리, 백업, 개인정보. 혼자 지는 부담입니다

### 필요한 조각

- 계정·인증
- 드릴/세션 공개 업로드 · 검색 · 태그
- 라이선스 표기 — 올린 사람이 무엇을 허락하는가
- 신고·차단·삭제 (운영 없이는 공개 업로드를 열 수 없습니다)
- 다국어 — 한국어 밖으로 나가는 첫 지점입니다
- 서버 비용과 백업 계획

---

## 결정이 필요한 것 (기현님)

**진도를 막고 있는 것들입니다.** 답이 있어야 해당 항목이 움직입니다.

| 무엇 | 지금 상태 | 물어보는 것 |
|---|---|---|
| **씨앗 드릴 3개** | 심는 장치는 완성(1회성 도장). **내용이 비어 있습니다** | 제목과 스텝 구성 |
| **설정 [팀] > '항상 묻기'** | 아무것도 묻지 않고 풀 코트로 엽니다. 문구만 사실로 되돌려 둔 상태 | 실제로 묻게 만들지, 항목을 없앨지 |
| **팀 색상 "코트 위 칩에 적용됩니다"** | 반쪽 진실입니다 — `drill.teams` 는 **생성 시점 스냅샷**이라 이미 만든 드릴은 안 바뀝니다 | 문구를 고칠지, 기존 드릴에도 소급할지 |
| **`CourtPreview.tsx`** | 프로덕션 소비자 0. 죽은 컴포넌트입니다 | 지울지, 되살릴 자리가 있는지 |
| **크롬 예산표와 `courtSize`** | §5.2/5.3 실측표가 전부 30×18 기준입니다. 28×15·25×14 는 축척이 다릅니다 | 표를 3단으로 늘릴지, 30×18 기준만 유지할지 |
| **28×15 를 기본으로 올릴지** | 기본은 `'30x18'` 입니다. 실제 경기장은 농구 코트(28×15)가 많습니다 | 기본값을 바꿀지 |
