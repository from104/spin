# SPIN 로드맵

> **이 문서가 "앞으로 할 일" 의 정본입니다.** `docs/PLAN-2026-08.md` 는 2026-08 재편의
> **역사 기록으로 동결**했습니다 — 거기 §7·§9 의 살아 있던 행은 전부 이 문서로 옮겼습니다.
> 앞으로 할 일을 두 곳에 적으면 두 곳이 갈라지고, 갈라지면 둘 다 못 믿게 됩니다.
>
> **다만 `docs/PLAN-2026-08.md` §8 「하지 않을 것」 20여 행은 그 문서에 그대로 둡니다** —
> 근거가 붙은 *기각 기록*이라 성격이 다릅니다. **새 기능을 제안하기 전에 그 절을 먼저
> 읽으십시오.** 이미 근거까지 붙여 기각한 것을 서너 달마다 다시 검토하는 것이 이 저장소가
> 겪은 낭비입니다.

**갱신**: 릴리스마다. 버전을 올릴 때 그 절을 닫고 다음 절을 엽니다.
**현재 버전**: 0.5.0 (2026-08-21) — 0.4(동기화·다국어)·0.5(미배송 빚) 를 한 릴리스로 묶었습니다.
0.4.0 은 따로 태그하지 않았습니다(실기 검증 전 devel 에서 0.5 작업이 바로 이어졌습니다).

---

## 큰 그림

> ⚠️ **2026-08-20 번호 재정렬**(기현님 결정). 옛 순서는 0.4 빚 → 0.5 Tauri → 0.6 동기화였는데,
> 드라이브 동기화·다국어가 먼저 완성되어 devel 에 랜딩됐습니다. 번호는 순서대로 쓰기로 하고
> (다음 릴리스 = 0.4.0), 옛 "0.6 동기화" 절의 내용이 0.4 로, 빚·Tauri 가 한 칸씩 뒤로
> 밀렸습니다. 모바일은 0.7 그대로입니다.

| 버전 | 성격 |
|---|---|
| **0.3** | 현장 피드백 — 코트에서 걸리는 것을 계속 고칩니다 |
| **0.4** | 구글 드라이브 동기화 + 다국어(한·영·일) — 구현 완료, 실기 검증 대기 |
| **0.5** | 미배송 빚 — *"저장은 되는데 화면엔 없다"* 를 닫습니다 |
| **0.6** | Tauri 데스크톱 — 리눅스·윈도우·맥 |
| **0.7** | 모바일 — 안드로이드 → 아이패드 순 |
| **1.0** | 전 플랫폼 배송 + 실기 매트릭스 통과 |
| **2.0** | 포털 — 드릴·세션 커뮤니티 |

### ⚠️ "서버 없음" 원칙은 단계적으로 풉니다

`docs/REQUIREMENTS.md` §1 과 README 는 **2026-08-20 에 함께 고쳤습니다** — "로컬 전용" 이
"로컬이 원본, 앱 서버 없음 + 선택적 내 계정 동기화" 가 됐습니다.

| 단계 | 상태 |
|---|---|
| 0.4 | **내 계정 동기화(랜딩됨).** 브라우저가 **사용자의** 구글 드라이브에 직접 씁니다 — 내 서버도 내 DB 도 없고, 남의 드릴을 내가 보관하지 않습니다. 로컬이 여전히 원본입니다 |
| 2.0 | **진짜 서버.** 커뮤니티에는 계정·검색·신고가 필요하고, 그건 서버 없이는 안 됩니다 |

---

## 0.3 — 현장 피드백

지금까지와 같은 방식입니다. 코트에서 써 보고 걸리는 것을 고칩니다. **이 절은 미리 다 채우지
않습니다** — 실기에서 나오는 것이 들어옵니다. 아래는 지금 알고 있는 것입니다.

- 판 네 모서리의 둥근 부분(약 5 px)에서 벤치로 되돌리기가 안 먹는 것
- 좁은 세로 화면(폭 774 px 미만)에서 벤치 띠가 3줄이 되어 도구 줄이 처음에 안 보이는 것
- 거리 원(3 m/5 m) 순환에 **키보드 경로가 없는 것** — 지금은 포인터 전용입니다
- 규칙 존(골 지역 표시)이 **인쇄물에 안 실리는 것**
- 새로고침하면 되돌리기 이력이 사라지는 것 · 줌·화면 이동이 100% 로 돌아오는 것
- 0.2.0 에서 바꾼 것들의 실기 확인 — 메모 모달이 코트를 가리는 정도, 태블릿에서 더블탭이
  실제로 열리는지, 진영 깃발이 멀리서 읽히는지, 트레이 드롭 예고가 체육관에서 보이는지

## 0.4 — 구글 드라이브 동기화 + 다국어

**구현은 완료되어 devel 에 있습니다**(동기화 8커밋 + 다국어 C1~C8, 2026-08-19~20).
CHANGELOG [Unreleased] 가 사용자 관점 설명을 갖고 있습니다. 릴리스 전에 남은 것:

- [ ] **Google Cloud OAuth Client ID 발급 + `VITE_GOOGLE_CLIENT_ID` 주입** — 기현님 손.
  사설 IP 에서는 OAuth 가 안 되므로 실기는 localhost 포워딩 또는 spin.atit.dev 로
- [ ] **동기화 실기 검증** — 두 기기 왕복, 충돌(나중 활동 승), 삭제 전파, 오프라인 복귀
- [ ] 다국어 실기 확인 — 영어·일본어로 주요 화면 훑기

구현된 범위(옛 "0.6" 절이 약속한 것 전부): 앱 전용 폴더 권한만 · 드릴/세션/명단 동기화
(**설정 제외** — 수정시각이 없어 최신 승 판정 불가, 접근성 설정이 조용히 꺼지는 사고 방지) ·
충돌은 가장 최근 활동 승 · 오프라인 우선 · 연결 끊어도 로컬 무손상.

## 0.5 — 미배송 빚

*"저장은 되는데 화면엔 없다"* 는 이 저장소의 고유 함정입니다. 값이 저장·마이그레이션·검증·
백업까지 왕복하는데 화면에 한 픽셀도 없는 상태입니다. 전수 색출이 찾은 5건은 **전부
구현 완료**(2026-08-20, 순차 5커밋 ff146b2…a18dfb2) — 나머지 코드 위생 2건만 남았습니다.

- [x] **팀 이름 입력** — 드릴 메타 시트(ⓘ [드릴 정보])에 홈·어웨이 팀 이름 입력을 추가했습니다.
  드릴 자신의 `teams[side].label` 을 바로 고치므로 색상과 달리 스냅샷 문제가 없습니다.
  ⚠️ 골키퍼 **색**은 제외 — 색상 변경 기능 자체는 폐기했습니다(아래 참조)
- [x] **세션 메모 · 항목 메모 · 휴식 시간 입력** — 세션 편집 화면에 세션 메모(상시)와 항목별
  메모·휴식 시간(토글 펼침)을 추가했습니다. `PrintSessionPlan.tsx` 가 이미 그 값을 그리던
  열/문단이 이제 채워집니다
- [x] **드릴 삭제 앞 '세션에서 사용 중' 경고** — 세션에서 쓰이는 드릴은 삭제 전 확인 모달에
  어느 세션인지 보여줍니다. ⚠️ 2026-08-20 삭제 안전망(아래 [`docs/PLAN-DELETE-SAFETY.md`](docs/PLAN-DELETE-SAFETY.md))
  으로 확인 자체가 **모든 드릴에 무조건** 걸리도록 바뀌었습니다 — 이 항목은 "경고 문구가
  참조 유무로 갈린다" 는 부분만 여전히 유효합니다
- [x] **메모 크기·색 입력** — `NoteEditModal`(메모 쓰기/수정)에 크기 3단·색 4종을 추가했습니다.
  렌더링에 숨어 있던 함정(줄 간격이 `size` 고정 전제였던 것)을 먼저 고쳐서 큰 글자로도
  여러 줄이 안 겹칩니다(소비자 5곳 — 화면·인쇄·PNG·썸네일·시연 — 전부 맞춤)
- [x] **선수 실명을 종이·PNG 에 태우기** — 인쇄 첫 장과 PNG 캡션 셋째 줄(있을 때만)에 실명을
  적은 선수만 나열합니다. `model/chairLabel.ts namedRosterOf` 가 시연 범례와 공유하는
  단일 출처입니다
- [x] **도움말·튜토리얼** (2026-08-20 설계·구현, 정본 [`docs/PLAN-HELP-TUTORIAL.md`](docs/PLAN-HELP-TUTORIAL.md)) —
  6개 화면(드릴 편집·자유 전술판·드릴 목록·세션 목록·세션 편집·시연) 전부에 첫 방문
  스포트라이트 튜토리얼(자동 시작, 화면별 1회성)과 문서형 도움말(HelpCenter, 8섹션)을
  붙였다. 도움말은 왼쪽 레일(좁은 창은 헤더)의 상시 [도움말] 칸 하나로 일원화됐고
  (HelpTriggerProvider), 각 섹션 끝의 [이 화면 투어 다시 보기]로 튜토리얼을 재시작할 수
  있다. 설정 맨 아래 [도움말·튜토리얼] > [모두 다시 보기]는 전체 플래그를 지운다. 1.0 의
  "사용 설명" 항목을 앞당긴 것
- [x] **삭제 안전망 — 스텝·드릴·세션·세션 편성** (2026-08-20 설계·구현, 정본
  [`docs/PLAN-DELETE-SAFETY.md`](docs/PLAN-DELETE-SAFETY.md)) — 기현님 신고("스텝·드릴·세션
  항목을 지우는 게 너무 쉽고 복구할 방법이 없다")로 시작. 원칙 하나로 네 곳을 갈랐다 —
  되돌릴 수 있으면 묻지 않고 알려주고, 되돌릴 수 없거나 놓쳤을 때 잃는 게 크면 묻는다.
  드릴 편집 스텝 삭제는 단일이면 undo 토스트만(Ctrl+Z 로도 돌아간다 — 이미 있었는데
  아무도 안 알려줬다), 일괄(N장)이면 확인 모달도. 드릴 목록·세션 목록의 삭제는 손실이 커서
  무조건 확인 + 8초 undo 토스트로 대칭을 맞췄다(세션 삭제는 이전엔 안전망이 전혀 없었다).
  세션 편집의 구획 삭제·편성 항목 제거는 화면 단위 스냅샷 되돌리기로 덮었다. 곁다리로 진짜
  버그 하나도 고쳤다 — 드릴/세션 삭제 undo가 동기화 톰스톤을 안 지워 되살린 문서가 다음
  동기화에 다시 사라지던 것. 휴지통(복원 보관함)은 검토 후 폐기 — "되살아난 스텝·드릴이
  돌아갈 자리가 없다"(종속이 시간이 갈수록 어긋난다)는 기현님 판단
- **죽은 export 청소** — `cubicBezier`·`pxToM`·`upcomingSession` 등
- **`PresentRunner` 의 `effectiveReduceMotion` 중복 구현 제거** — 판정식이 두 벌입니다

### 2026-08-20 결정 반영 (기현님, 10문답)

옛 "결정이 필요한 것" 표의 6건이 전부 답을 받았습니다. 이어서 미배송 빚 목록 자체도
정리했습니다(기현님: "드릴 삭제 경고·선수 실명 인쇄·세션/항목 메모·팀 이름 입력만 살리고
다 소스에서 폐기·삭제"). 실행 항목은 이 절에 담습니다:

- [ ] **씨앗 드릴 폐기** — 내용을 채우지 않기로. 심는 장치(1회성 도장)와 관련 코드·문서
  언급을 걷어냅니다. 1.0 절의 "씨앗 드릴" 행도 함께 지웁니다
- [x] **설정 [팀] '항상 묻기' 를 실제로 구현** — **2026-08-21 번복.** 2026-08-20 결정 당시엔
  "안 묻는 게 문제니 진짜로 묻게 만들자" 였지만, 다시 보니 이 값의 유일한 소비처가 전술판
  부팅 1회뿐이라 애초에 물어볼 이유가 없었다 — 구현하는 대신 **행 자체를 폐기**했다
  (880bb47, 위 "`defaultFormation`·`defaultCourtMode`·`hints.iosPwa` 폐기" 참고)
- [x] **팀 색상 변경 기능 폐기** (2026-08-21 이행) — `drill.teams` 가 생성 시점 스냅샷이라
  "칩에 적용됩니다" 문구가 반쪽 진실이던 문제를, 소급이 아니라 **기능 제거**로 닫았습니다.
  설정의 [팀] 섹션(색상 2행)·TeamColorSwatches·`prefs.teams` 필드·TEAM_COLOR_NAMES 를
  걷어냈고, 새 판의 팀은 로케일 기본값으로 태어납니다(팀 **이름**은 드릴 편집 ⓘ 시트 담당
  — §0.5 미배송 빚에서 이행 완료). 옛 저장본의 teams 는 validatePrefs 에서 소리 없이
  증발하고, 이미 만든 드릴·전술판의 색은 각자의 스냅샷 그대로 유지됩니다
- [ ] **`CourtPreview.tsx` 삭제** — 프로덕션 소비자 0 인 죽은 컴포넌트. 되살릴 일이 생기면
  git 이력에서 꺼냅니다
- [x] **저장소 열화·압박 경고 배너 폐기** — `storage/db.ts` 의 `ensurePersistence`·
  `storagePressure`, `prefs.ts` 의 `hints.degradedStorage` 를 지웠습니다(옛 `iosPwa` 힌트는
  별개 기능이라 유지). `docs/DESIGN.md` 세 곳에도 폐기 각주를 남겼습니다
- [x] **`defaultFormation`·`defaultCourtMode`·`hints.iosPwa` 폐기** (2026-08-21 기현님 지시,
  설정 화면 감사 후속) — 포메이션 기본값은 코치 재량 침범이라 걷어냈고([포메이션으로
  채우기]·세트피스는 드릴에 새겨진 formation 을 씁니다), 기본 코트 모드는 전술판 스냅샷이
  코트를 기억해 기기당 최초 1회만 읽히는 유령 설정이었으며, iosPwa 는 §6.9 배너가 끝내
  미구현이라 플래그만 백업을 타고 다녔습니다. 옛 저장본의 세 필드는 validatePrefs
  화이트리스트에서 소리 없이 증발합니다(스키마 도장 불변)
- [x] **`propagateForward` 삭제** — `model/edits.ts` 에서 함수와 이제 안 쓰이는 헬퍼
  `poseOfCast` 를 함께 지웠습니다(같이 쓰이던 `posesEqual` 은 다른 곳에서도 써서 유지)
- [x] **공·콘·메모 테두리를 `strokeFor` 로 넓히는 계획 취소** — 칩(`ChairChip.tsx`)만
  `strokeFor` 를 쓰고 공·콘·메모는 여전히 흰색 고정입니다. 지울 죽은 코드가 없는 "미착수
  확장" 이라 항목만 걷어냈습니다 — `strokeFor` 자체는 칩에서 계속 씁니다

크롬 예산표는 **30×18 기준 유지**, 기본 코트 크기도 **30×18 유지**로 확정 — 둘 다 코드
변경이 없는 "그대로" 결정이라 실행 항목이 아닙니다.

> ⚠️ **첫 화면 표적 예산 ≤ 40 이고 지금 여유가 0 입니다**(초기 37 / 서랍 둘 다 열림 40).
> 위에서 컨트롤을 더하는 항목은 **반드시** 인스펙터·시트·모달·서랍 안으로 들어가야 합니다.
> `src/test/boardTargetBudget.test.tsx` 가 게이트입니다.

## 0.6 — Tauri 데스크톱

리눅스·윈도우·맥. 데스크톱을 먼저 하는 이유는 **심사도 스토어 계정도 서명도 없어서** 입니다 —
패키징·자동 업데이트·파일 접근을 여기서 익히고 모바일로 갑니다.

- Tauri 셸 + 빌드 파이프라인 (3 OS)
- 파일 열기/저장을 **네이티브 대화상자**로 — 브라우저 다운로드 폴더를 거치지 않습니다
- 자동 업데이트
- 창 크기·전체화면 상태 기억
- 앱 아이콘·설치 패키지(deb·AppImage·msi·dmg)
- 웹앱과 **같은 코드 한 벌** 유지 — 분기는 최소로

## 0.7 — 모바일: 안드로이드 → 아이패드

> ⚠️ **대상 기기 제한**(2026-08-21 기현님 지시). **폰(일반 스마트폰)은 대상이 아닙니다** —
> 코트 전체를 보면서 개체를 세밀하게 조작하는 전술판이라, 화면이 작으면 앱 특성상 쓸 수가
> 없습니다. **7인치 이상 태블릿, 또는 폴드폰(펼친 상태)** 만 지원합니다 — 접힌 폴드폰은
> 폰과 같은 취급이라 대상이 아니고, 펼친 화면으로만 실기 검증합니다. 새로 정하는 숫자가
> 아니라 기존 기준을 스토어 배포에도 그대로 적용하는 것이다 — 7인치 태블릿은 이미
> DESIGN.md §6.4 각주(2026-08-13)가 "이 앱의 1순위 기기"로 못박은 실측 기준점(배율
> 0.663)이다. 실행에 옮길 것:
> - 스토어 등록의 기기 요건에 최소 화면 크기(7인치)를 명시 — Android 는 Play Console
>   "지원 기기" 목록에서 폰을 제외, **iOS 는 iPad 전용 배포로 확정**
> - `docs/FIELD-TEST.md` 기기 매트릭스에 폰 행을 넣지 않는다 — 태블릿·폴드폰(펼침)만
>
> **포팅 순서**(2026-08-21 기현님 지시): 0.6 데스크톱(윈도우·리눅스·맥OS) 을 먼저 끝내고,
> 모바일은 **안드로이드 → 아이패드** 순으로 포팅합니다. 근거: **기현님이 안드로이드
> 사용자다** — 실기 검증을 본인 일상 기기에서 바로 할 수 있는 쪽을 먼저 간다.

- 모바일 셸(Tauri Mobile 또는 대안 — 0.6 결과를 보고 정합니다)
- 스토어 계정·서명·심사 대응 — **안드로이드(Play Console) 먼저**, 아이패드(App Store) 는 뒤
- 태블릿 가로/세로 실기 재검증 — 안드로이드 태블릿부터, 아이패드는 뒤
- 화면 꺼짐 방지·전체화면이 네이티브에서도 되는지 확인
- 파일 공유(Android 인텐트를 먼저, iOS 공유 시트는 아이패드 단계에서)

## 1.0 — 전 플랫폼 배송

- 웹·데스크톱 3종·모바일 2종이 **같은 버전으로** 나갑니다
- `docs/FIELD-TEST.md` 기기 매트릭스를 실제로 채웁니다 (지금은 비어 있습니다)
- ~~사용 설명 — 앱 안에서 읽는 짧은 안내~~ → **0.5 로 앞당김** — 2026-08-20 기현님 지시로
  문서형 도움말 + 화면별 튜토리얼(스포트라이트) 설계가 확정됐다. 정본은
  [`docs/PLAN-HELP-TUTORIAL.md`](docs/PLAN-HELP-TUTORIAL.md)

> 씨앗 드릴은 2026-08-20 기현님 결정으로 **폐기**했습니다(0.5 절의 결정 반영 참조) —
> 빈 앱은 빈 채로 시작합니다.

## 2.0 — 포털

드릴과 세션을 전 세계 코치가 주고받는 곳. **여기서 "서버 없음" 이 끝납니다.**

### 무엇이 참이어야 시작하는가 (전제조건)

이 셋이 안 되면 포털은 빈 서버가 됩니다. **하나라도 아니면 시작하지 않습니다.**

1. **1.0 이 실제로 여러 기기에서 돌고 있다** — 만들 것이 아니라 쓰고 있는 앱이어야 합니다
2. **내 팀 밖의 코치가 SPIN 으로 드릴을 만들고 있다** — 공유할 것이 없으면 공유할 곳도 필요 없습니다
3. **운영을 감당할 수 있다** — 서버 비용, 신고 처리, 백업, 개인정보. 혼자 지는 부담입니다

### 필요한 조각

- 계정·인증
- 드릴/세션 공개 업로드 · 검색 · 태그
- 라이선스 표기 — 올린 사람이 무엇을 허락하는가
- 신고·차단·삭제 (운영 없이는 공개 업로드를 열 수 없습니다)
- 다국어 — 한국어 밖으로 나가는 첫 지점입니다
- 서버 비용과 백업 계획

---

## 버전 미정 — 새 기능 후보

**갱신**: 기현님이 새 아이디어를 주실 때마다. 버전 번호가 정해지면 해당 절로 옮깁니다.

- [ ] **보드를 이용한 움직이는 규칙 설명 화면**(2026-08-21 제안) — 전술판 엔진(물리·재생·
  보간)을 재사용해 2-on-1·골 지역 2인·세트피스 5m 같은 규칙을 실제로 움직이는 장면으로
  보여준다. 처음 파워체어풋볼을 접하는 코치·심판·선수에게 지금의 텍스트 설명보다 훨씬
  빠르게 전달될 것으로 보인다. 서버가 필요 없어 2.0 포털을 기다릴 이유가 없다 — 독립적으로
  착수 가능
- [ ] **여러 팀을 관리하는 화면**(2026-08-21 제안) — 지금 설정의 [선수 명단]은 팀 하나를
  전제한다. 여러 팀(클럽·연령대별 스쿼드 등)을 다루는 코치를 위해 별도 화면을 만들고,
  현재 설정 안에 있는 관련 기능(명단 등)을 그리로 옮긴다. 설계 시 결정할 것: 드릴·세션이
  여러 팀 중 어느 팀 소속인지 표시할지, 화면 간 팀 전환이 필요한지
- **기기 간 데이터 공유·여러 팀 관리는 모바일 한정 부분 유료화 추진**(2026-08-21 결정) —
  구글 드라이브 동기화(0.4 로 이미 배송)와 위 여러 팀 관리 화면, 이 둘을 **모바일 앱(0.7
  이후)에서만** 일부 유료로 전환하는 방향으로 간다. 데스크톱·웹은 계속 무료다. 과금 방식
  (스토어 인앱결제·구독 등)과 무료/유료 경계는 미정 — 0.7 착수 시점에 다시 결정한다

---

## 결정이 필요한 것 (기현님)

**비어 있습니다** — 2026-08-20 10문답으로 여섯 건이 전부 답을 받았습니다(결정 내용과 실행
항목은 0.5 절 "2026-08-20 결정 반영"). 새 질문이 생기면 여기 표로 다시 쌓습니다.

<details>
<summary>닫힌 결정 (2026-08-20)</summary>

| 무엇 | 결정 |
|---|---|
| 씨앗 드릴 3개 | **폐기** — 심는 장치까지 걷어낸다 |
| 설정 [팀] > '항상 묻기' | **실제로 묻게 구현** → 2026-08-21 번복, 행 자체를 폐기(§0.5 참고) |
| 팀 색상 "칩에 적용됩니다" | **색상 변경 기능 폐기** (팀 이름 입력은 미배송 빚대로 진행) |
| `CourtPreview.tsx` | **삭제** |
| 크롬 예산표와 `courtSize` | **30×18 기준 유지** |
| 28×15 기본 승격 | **30×18 유지** |

같은 문답에서 확정된 운영 결정: 다음 릴리스는 **0.4.0**(번호 재정렬 — 큰 그림 표 참조) ·
CHANGELOG [Unreleased] 는 **작업 직후 바로바로 정리** · README/REQUIREMENTS 의 "서버 없음"
문구는 **랜딩 즉시 수정**(완료) · 다음 개발 착수는 **0.5 미배송 빚**.

</details>
