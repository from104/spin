{
  "name": "spin",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "oxlint",
    "preview": "vite preview",
    "_comment:tauri": "데스크톱(Tauri) 셸. tauri:dev 는 vite dev 서버를 띄운 뒤 네이티브 창을 붙이고, tauri:build 는 `npm run build` 로 dist 를 만든 뒤 설치 패키지(리눅스=deb·rpm·AppImage)를 src-tauri/target/release/bundle 아래에 낸다. 윈도우·맥 산출물은 각 OS 에서만 나오므로 CI(.github/workflows/desktop-release.yml) 가 맡는다.",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "test": "vitest run",
    "test:watch": "vitest",
    "_comment:test:rel": "고친 파일을 import 하는 테스트만 돈다(모듈 그래프 추적). 전체는 55초, 이쪽은 보통 2~5초다 — 커밋 직전 한 번만 `npm test` 를 돌리고 그 전까지는 이것을 쓴다. 예: npm run test:rel src/render/SideMarks.tsx",
    "test:rel": "vitest related --run",
    "_comment:deploy": "spin.atit.dev 로 배포한다(정적 파일을 cube 의 /pool/vhost/sites/spin 에 놓는 것이 전부다). gofu·cube 어느 쪽에서 실행해도 된다 — 대상 경로가 로컬에 있으면 복사, 없으면 ssh 를 태운다. 먼저 `npm run deploy -- --dry-run` 으로 무엇이 바뀌는지 보라.",
    "deploy": "bash scripts/deploy.sh",
    "_comment:deploy:aws": "spin.atit.app 으로 배포한다(AWS Lightsail, mocil 과 같은 서버의 별도 Apache vhost). PEM 키로 ssh+rsync. 먼저 `npm run deploy:aws -- --dry-run` 으로 확인하라.",
    "deploy:aws": "bash scripts/deploy-aws.sh",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-opener": "^2.5.4",
    "idb": "^8.0.3",
    "matter-js": "^0.20.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router": "^8.3.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@testing-library/jest-dom": "^7.0.0",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.3",
    "@types/matter-js": "^0.20.2",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "@vitest/ui": "^4.1.10",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1",
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
