{
  "name": "spin",
  "private": true,
  "version": "0.6.12",
  "license": "MIT",
  "homepage": "https://spin.atit.app",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/from104/spin.git"
  },
  "bugs": {
    "url": "https://github.com/from104/spin/issues"
  },
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && npm run seo",
    "_comment:seo": "SEO C2 — 규칙 해설을 언어별 정적 HTML 로 굽고(3언어 × 11장) sitemap.xml 을 만든다. 앱은 클라이언트 렌더라 크롤러가 2차 크롤에서야 보는데, 규칙 글은 고정 텍스트라 미리 구워 두면 그 불확실성이 사라진다. 노드가 TS 를 못 읽어 vite 의 SSR 빌드를 한 단계 거친다(tsx·esbuild 같은 새 의존성을 안 들이려고). build 가 끝에 부르므로 따로 칠 일은 없다.",
    "seo": "vite build --ssr src/seo/prerenderData.ts --outDir .seo-build --logLevel warn && node scripts/prerender.mjs",
    "lint": "oxlint",
    "_comment:lint:rel": "고친 파일만 린트한다(HEAD 이후 바뀐 .ts/.tsx, 또는 인자로 준 파일). 전체는 고칠 계획이 없는 오랜 경고 46줄을 함께 뱉어서 방금 만든 한 줄이 그 속에 묻힌다 — 이쪽은 0.3초에 끝나고 출력이 비어 있으면 깨끗한 것이다. test:rel 과 같은 짝이고, 전체 lint 는 커밋 직전 한 번이면 된다. 예: npm run lint:rel src/app/AppRail.tsx",
    "lint:rel": "bash scripts/lint-rel.sh",
    "preview": "vite preview",
    "_comment:tauri": "데스크톱(Tauri) 셸. tauri:dev 는 vite dev 서버를 띄운 뒤 네이티브 창을 붙이고, tauri:build 는 `npm run build` 로 dist 를 만든 뒤 설치 패키지(리눅스=deb·rpm·AppImage)를 src-tauri/target/release/bundle 아래에 낸다. 윈도우·맥 산출물은 각 OS 에서만 나오므로 CI(.github/workflows/desktop-release.yml) 가 맡는다.",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "_comment:android": "안드로이드(Capacitor 8) 셸. 설정은 루트 capacitor.config.ts 이고 네이티브 프로젝트는 src-android/ 다(src-tauri/ 와 짝). android:build 는 `npm run build` 로 dist 를 만들고(SPIN_ANDROID_BUILD=1 이 vite 의 envPrefix 에 SPIN_ANDROID_ 를 켠다 — 이 신호가 없으면 클라이언트 ID·웹 출처가 번들에 안 들어간 채 초록으로 나간다) `cap sync` 로 그 dist 와 플러그인 목록을 네이티브에 옮긴 뒤 scripts/android-build.sh 가 gradle 을 돈다(서명 env 4개가 다 있으면 릴리스 APK+AAB, 없으면 디버그 APK). android:dev 는 빌드 없이 기기에 붙여 vite 개발 서버를 그대로 띄운다 — 주소는 커밋하지 않고 SPIN_CAP_SERVER_URL 로 준다(예: SPIN_CAP_SERVER_URL=http://100.75.15.13:5173 npm run android:dev). cap run 이 먼저 sync 를 돌리므로 capacitor.config.ts 의 server.url 과 cleartext 가 그대로 실린다(평문 차단도 그때 풀린다). ⚠️ 라이브 리로드 플래그 `-l` 을 붙이지 않는다: CLI 가 server.url 을 제 값(LAN IP + 포트 3000)으로 덮어써 SPIN_CAP_SERVER_URL 이 무시되고, 앱이 vite 가 없는 :3000 을 본다. `--external` 은 cap run 에 아예 없는 옵션이라 곧장 죽는다(Capacitor CLI 8.2.0, 2026-09-17 검수). 굳이 -l 을 쓰려면 `-l --port 5173`. android:open 은 Android Studio 로 프로젝트를 연다(SDK 설치·AVD·로그 보기용).",
    "android:build": "SPIN_ANDROID_BUILD=1 npm run build && npx cap sync android && bash scripts/android-build.sh",
    "android:dev": "npx cap run android",
    "android:open": "npx cap open android",
    "_comment:android-assetlinks": "App Links 검증 파일(public/.well-known/assetlinks.json)을 찍는다 — 이 파일이 있어야 https://spin.atit.app/s/… 링크가 브라우저가 아니라 앱으로 열린다. 인자는 설치분을 서명한 모든 키의 SHA-256 지문이다(업로드 키·Play 앱 서명 키·디버그 키). 지문이 아직 없어 파일 자체는 저장소에 없다 — 키스토어가 생긴 뒤 찍어서 커밋하고 `npm run deploy:aws` 로 내보낸다. 파일을 손으로 고치지 말고 인자를 늘려 다시 찍는다. 예: npm run android:assetlinks -- AB:CD:…:EF",
    "android:assetlinks": "node scripts/android-assetlinks.mjs",
    "_comment:share:dev": "공유 링크 백엔드(server/share)를 개발용으로 띄운다. Node ≥22.18 이 .ts 를 타입만 지우고 바로 돌리므로 빌드도 의존성도 없다. 기본 127.0.0.1:8787 이고 데이터는 ./data/share (gitignore). `npm run dev` 의 Vite 프록시가 /api 를 이쪽으로 넘기므로, 앱은 개발에서도 운영과 똑같이 상대 경로 /api/share 만 부른다. 환경변수 표는 server/share/README.md.",
    "share:dev": "node server/share/index.ts",
    "_comment:share:test": "공유 링크 백엔드만 좁혀서 돈다(server/share/app.test.ts). 전체 `npm test` 도 같은 파일을 돌지만(vite.config.ts 의 test.include), 서버만 고쳤을 때 55초를 기다릴 이유가 없다.",
    "share:test": "vitest run server/share",
    "test": "vitest run",
    "test:watch": "vitest",
    "_comment:test:rel": "고친 파일을 import 하는 테스트만 돈다(모듈 그래프 추적). 전체는 55초, 이쪽은 보통 2~5초다 — 커밋 직전 한 번만 `npm test` 를 돌리고 그 전까지는 이것을 쓴다. 예: npm run test:rel src/render/SideMarks.tsx",
    "test:rel": "vitest related --run",
    "_comment:deploy": "spin.atit.dev 로 배포한다(정적 파일을 cube 의 /pool/vhost/sites/spin 에 놓는 것이 전부다). gofu·cube 어느 쪽에서 실행해도 된다 — 대상 경로가 로컬에 있으면 복사, 없으면 ssh 를 태운다. 먼저 `npm run deploy -- --dry-run` 으로 무엇이 바뀌는지 보라.",
    "deploy": "bash scripts/deploy.sh",
    "_comment:deploy:aws": "spin.atit.app 으로 배포한다(Lightsail 인스턴스의 별도 Apache vhost). PEM 키로 ssh+rsync. 먼저 `npm run deploy:aws -- --dry-run` 으로 확인하라.",
    "deploy:aws": "bash scripts/deploy-aws.sh",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@capacitor-community/keep-awake": "8.0.1",
    "@capacitor/android": "8.2.0",
    "@capacitor/app": "8.1.1",
    "@capacitor/browser": "8.0.4",
    "@capacitor/core": "8.2.0",
    "@capacitor/filesystem": "8.1.3",
    "@capacitor/preferences": "8.0.1",
    "@capacitor/share": "8.0.2",
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-opener": "^2.5.4",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.11.0",
    "h264-mp4-encoder": "^1.0.12",
    "idb": "^8.0.3",
    "matter-js": "^0.20.0",
    "mediabunny": "^1.55.7",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router": "^8.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.2.0",
    "@tauri-apps/cli": "^2.11.4",
    "@testing-library/jest-dom": "^7.0.0",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.3",
    "@types/matter-js": "^0.20.2",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "@vitest/ui": "^4.1.10",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1",
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
