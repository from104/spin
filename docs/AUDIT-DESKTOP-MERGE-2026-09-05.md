# 데스크톱 병합(e053728) 8커밋 실효성 감사 — 2026-09-05

> 기현 지시: *"이번 커밋으로 고쳐진 소스의 실제 효용성 감사"*. 감사관 5명(코트·깃발·AppImage 는 각자 worktree 에서 돌연변이 재현, OAuth·CI 는 읽기 전용) + 합성관 교차검증. 워크플로우 wf_83785057-b64. 증거 스크립트·스크린샷은 세션 스크래치에 있어 저장소에는 결론만 남긴다.

대상: 병합 `e053728` (2026-09-05) 이 devel 에 들여온 `from104/tauri-desktop` 8커밋
감사관 5명(관점 A~E) + 합성관 교차검증. 트리 상태: **`git status --short` 출력 없음(깨끗)** — 합성 단계는 읽기 전용, 파일 수정 0.

---

## ① 판정표

| 커밋 | 주장 | 판정 | 근거 한 줄 |
|---|---|---|---|
| `9401ca5` | Tauri 셸 + 3-OS CI 파이프라인 | **부분 효용** | 리눅스 deb/rpm/AppImage 는 실제로 나오고 셸도 뜨지만, `git cat-file -e v0.6.3:.github/workflows/desktop-release.yml` = **ABSENT** · `git branch -r --contains 9401ca5` = 빈손 — CI 는 한 번도 돈 적이 없고 현재 태그로는 영원히 안 돈다(합성관 재확인) |
| `5a4e81e` | GTK_IM_MODULE=xim 으로 AppImage SIGSEGV 수리 | **부분 효용** | 진단·처방 모두 A/B 실증(exit 139→124)되었으나 **실행 코드가 최종 트리에 0줄** — `main.rs` 에 남은 것은 "xim 은 얼어붙는다, 고치지 마시오" 경고 주석뿐(합성관이 `main.rs` 전문 확인) |
| `90c57af` | AppImage+웨일랜드일 때만 x11 강제를 걷어내 SIGSEGV 제거 | **실증됨** | 같은 바이너리 환경변수 A/B: APPDIR 없음=139 SIGSEGV ↔ 가드 발동=124 8초 생존, 실제 번들 실행 시 `im-wayland.so` 로드·`xlsclients`=0(진짜 웨일랜드 표면) |
| `63e46eb` | 코트를 높이 기준으로 — 넓은 창 아래 잘림 수리 | **부분 효용** | 13개 뷰포트 크로미움 실측에서 **주장된 overflow 잘림이 재현 안 됨**(수정 전후 clipBelowCard 동일 −66). 그러나 같은 코드가 다른 실제 버그를 막는다 — 넓은 창에서 SVG 히트면이 카드 폭 전체를 덮어 **코트 밖 클릭이 공을 코트 밖에 놓던 것**(CDP 실클릭 재현) |
| `63d7b82` | 90° 회전에도 깃발이 화면에서 제 모양 유지 | **실증됨** | 700×1200 에서 스테이지가 실제로 `rotate(90)` 하고 깃발 4개는 화면 좌표 기준 깃대 세로 유지 · 돌연변이 3건 전부 빨간불 · 12조합 기하 재계산에서 0.5 m 간격·viewBox 이탈 0 |
| `85c20e2` | CSP 가 구글 로그인 스크립트를 막고 있다 | **무효·유해** | 데스크톱은 GIS 를 **한 번도 요청하지 않는다**(실측 `script[src*=accounts.google.com]`=0). 같은 날 1시간 17분 뒤 `29936be` 가 자기 전제를 죽였다. 관점 D·E 가 독립적으로 같은 결론 |
| `bf0b4c1` | 릴리스에 devtools — 진단 창구, 무해 | **부분 효용** | `cargo tree -e features -i tauri` 로 feature 활성은 확인. 리눅스 WebKitGTK 릴리스 창에서 **인스펙터가 실제로 열리는지는 미확인 — 근거 부족**(안 열리면 효용 0) |
| `29936be` | 데스크톱 드라이브 로그인(외부 브라우저+루프백+PKCE) | **부분 효용** | PKCE·state·refresh 저장은 돌연변이 3건으로 실증. 그러나 **배포 상태로는 브라우저가 아예 안 열린다** — 합성관이 크레이트 원본 재확인: `allow-open-url` = "without any pre-configured scope", `is_url_allowed` 는 `allowed.iter().any(…)` 라 빈 목록이면 false, `tauri.conf.json` 에 opener scope 키 없음 |

---

## ② 감사관들이 어긋난 자리

**실질 충돌은 없었다.** 겹친 자리에서는 오히려 독립 확증이 나왔다.

1. **85c20e2 — D·E 독립 합치.** 관점 E 가 곁가지로 "죽은 수정일 가능성"을 지적했고, 관점 D 가 임시 테스트로 실측해 확정했다. 서로 모른 채 같은 결론.
2. **tauri.conf `maximized:true` — A 는 63e46eb 의 스코프 이탈로, E 는 9401ca5 잔여 리스크로 각각 언급.** 합성관이 `git log -S` 로 확정: **63e46eb 가 넣었다.** `fix(board)` 커밋이 창 기동 동작을 함께 바꾼 것이 맞다 — E 의 서술이 커밋 귀속에서 느슨했다.
3. **AppImage 검증 범위 — C 와 E 는 다른 것을 봤고 둘 다 옳다.** C 는 로컬 빌드 AppImage 를 살렸고, E 는 CI 산 배포본이 존재한 적 없음을 밝혔다. 합치면 결론이 하나다: **검증된 AppImage 와 배송될 AppImage 는 아직 같은 물건이 아니다.**
4. **63e46eb 돌연변이의 충실성** — A 는 "두 줄만 되돌렸다"고 했다. 합성관이 실제 diff 를 확인: 코드 변경은 정확히 그 두 줄(카드 줄 + 코트 칸 줄)뿐이고 `flex:'0 1 auto'`·`minHeight:0` 은 기존값이다. **A 의 돌연변이는 충실했고, 따라서 "overflow 잘림 미재현"은 유효한 반증이다.**

---

## ③ 지금 당장 고쳐야 할 것

**[치명 1] 29936be — 배포 상태로 로그인이 시작조차 안 된다.**
`src-tauri/capabilities/default.json` 에 `opener:allow-default-urls` 또는 명시 scope(`https://accounts.google.com/*`)를 더하지 않으면 `openUrl` 이 `ForbiddenUrl` 로 거부된다. 테스트가 이를 못 본 이유는 플러그인을 통째로 목으로 갈아 끼우기 때문이다(`authDesktop.test.ts:13`).
같은 자리에서 **`CHANGELOG.md:11`** 의 *"데스크톱 앱 출시, 구글 드라이브 동기화 지원(로그인은 기본 브라우저에서 진행)"* 은 지금 사실이 아니다 — 실기 통과 전까지 보류.

**[치명 2] 29936be — 다음 `deploy:aws` 한 번에 데스크톱 client_secret 이 공개 번들로 나간다.**
`src/sync/auth.ts:21` 은 `import * as desktop from './authDesktop.ts'` — **정적** import 다. 그런데 `authDesktop.ts:35-36` 주석은 *"모듈 자체가 동적 import 라 번들도 따로 떨어진다"* 고 적어 자기 코드와 모순된다(합성관 확인). 관점 D 의 SENTINEL 빌드가 `assets/index-*.js` 에 시크릿이 박히는 것을 실증했다. 현재 라이브 번들은 아직 깨끗하다 — **다음 배포가 방아쇠다.**

**[치명 3] 85c20e2 — 되돌린다.**
`script-src` 의 `https://accounts.google.com` 과 `frame-src` 절 전체를 뺀다(`connect-src` 3종은 실사용이므로 유지). 이 죽은 완화가 이미 오독을 낳았다 — `secret_store.rs` 머리말이 아직 "지금 앱에 외부 스크립트가 하나 있고(구글 GIS)" 라고 적고 있다.

**[중 4] 90c57af — 가드가 방아쇠가 아니라 대리변수를 본다.**
`WAYLAND_DISPLAY` 를 키로 쓰지만 실제 방아쇠는 `x11 백엔드 + GTK_IM_MODULE=wayland` 다. 출시된 AppImage 를 `env -u WAYLAND_DISPLAY` 로 띄우면 **지금도 exit 139 로 죽는다**(실증). systemd 유저 유닛·일부 런처가 이 맥락이다. 더해 사용자가 의도적으로 x11 을 원할 탈출구가 **하나도 없다**(#8541 재발 시 회피 불가) — `SPIN_FORCE_X11=1` 을 둬라.

**[중 5] 63d7b82 — 소스가 뒤집힌 옛 교리를 그대로 말한다.**
`src/render/sideFlags.ts:52-54` 가 아직 *"화면에서는 깃대가 눕는다 — 되돌려 세울 것도 없다"* 고 적혀 있다(합성관 원문 확인). 커밋이 이 문단을 안 건드렸다. AGENTS.md §2·§3 위반이고, 다음 사람이 이 주석을 근거로 되돌림을 "버그"로 지울 수 있다. 함께 `rot === 90` 하드코딩을 `rot !== 0` 로 바꿔 되돌림과 자리 보정이 같은 도메인을 보게 한다.

**[중 6] 9401ca5 — CI 주장의 절반이 미집행이다.**
브랜치·워크플로를 실제로 푸시하고 태그를 다시 끊어야 처음 돈다. 함께 (a) `dry_run` 입력이 아무 데도 안 물려 있으니 배선하거나 지우고(선언부 19행 1히트뿐, 합성관 확인), (b) gate 에 `cargo check --manifest-path src-tauri/Cargo.toml` 를 넣어라 — 지금 gate 는 러스트를 한 줄도 안 본다.
`ROADMAP.md:17` 은 병합 뒤에도 *"devel 미병합"* 이라 적혀 있다.

**[하 7] 63e46eb — 근거를 실증된 것으로 갈아 끼운다.**
소스 주석의 "overflow 잘림" 서사는 크로미움 13개 크기 어디서도 재현되지 않았다. 실제로 지키는 것은 **"무대의 히트면이 코트 밖으로 새면 안 된다"** 이다. `board.test.tsx:127-131` 의 인라인 style 단언 4개는 AGENTS.md 「테스트 작성 규칙」이 명시적으로 금지한 종류(*"인라인 스타일 값(px·색 토큰 문자열)"*)이므로 **코트 레터박스 바깥 클릭이 오브젝트를 놓지 않는다**는 행동 테스트로 교체한다. `tauri.conf` 창 플래그는 보드 수정에서 떼어낸다.

**[하 8] 관점 밖이지만 넘길 수 없는 것.**
`secrets/*.json` 2개가 **git 에 추적 중**이다(웹·데스크톱 client_secret 원문, 커밋 `75f89ab` — 이 8커밋 소행 아님). `.gitignore:30` 의 `client_secret_*.apps.googleusercontent.com.json` 이 파일명 중간 공백(`… .com web.json`) 때문에 안 걸렸다(합성관 `git ls-files` 확인). 저장소는 PRIVATE(`gh repo view` 확인)이라 급하진 않으나, 패턴을 `secrets/` 로 바꾸고 히스토리 처리 여부를 기현님이 판단할 것.

---

## ④ 실기에서만 확인되는 것 (jsdom·헤드리스가 못 재는 것)

**윈도우·맥 — 증거 0.** 3-OS 매트릭스는 한 번도 안 돌았다. 서명·공증 없음. `ubuntu-22.04` 러너 고정과 `libappindicator3-dev`(Tauri 2 문서는 `libayatana-…`) 도 CI 가 돌기 전엔 검증 불가.

**한글 입력 — 어느 감사관도 못 쟀다.** GNOME 이 스크린샷 포털을 거부했고, 웨일랜드라 `wtype`/`xdotool` 로 앱에 키를 못 넣으며 `ydotool` 주입은 기현님 데스크톱을 가로챈다. `5a4e81e` 의 "xim 을 쓰면 창이 얼었다"는 **기현님 실기 보고가 유일한 근거이며 이 감사는 확증도 반증도 못 했다 — 근거 부족.** `90c57af` 이후 한글이 실제로 되는지도 미확인(구조적 정황만: 웨일랜드 백엔드 + `im-wayland.so` 는 이미 검증된 deb·dev 와 같은 짝).

**기현님이 볼 항목:**
1. **AppImage(WebKitGTK)를 2560×1440 최대화**해 전술판을 열고 코트 아래가 트레이에 가리는지 — 크로미움에서는 그 증상이 재현되지 않았고, 이 기기에 WebKit 엔진이 없어 검증 불가다.
2. 아주 넓은 창에서 **코트 좌우 빈 띠를 클릭** — 수정본에서는 공이 안 놓여야 한다.
3. `capabilities` 에 opener 스코프를 넣은 뒤 **[Google 계정 연결] → 기본 브라우저가 뜨는가**, 로그인 완료 시 앱이 '연결됨' 이 되는가(300초 대기에 빠지지 않는가).
4. 릴리스 AppImage 에서 **우클릭 → 요소 검사가 실제로 열리는가**(안 열리면 `bf0b4c1` 효용 0).
5. **CI(ubuntu-22.04)가 만든 AppImage**를 이 기계에서 한 번 띄울 것 — #8541 재발 여부는 그때 처음 알 수 있다.
6. 1024×768 처럼 폭이 물리는 크기에서 튜토리얼 [코트] 스포트라이트가 코트를 감싸는지.

---

## ⑤ 남길 것

**코드로 남길 것 (8커밋 중 실제 자산):**
- `90c57af` 의 디스플레이 백엔드 가드 — 실제 크래시를 실제로 없앤 유일한 수정.
- `63d7b82` 의 깃발 되돌림 — 5건 중 유일하게 반박이 안 붙은 **실증됨**.
- `63e46eb` 의 높이 기준 배치 — 이유는 틀렸지만 코드는 진짜 버그를 막는다.
- `9401ca5` 의 리눅스 패키징 — 산출물이 실재한다.
- `bf0b4c1` — `29936be` 의 스코프 문제를 잡을 때까지 **유일한 현장 진단 수단**이므로 먼저 지우지 말 것.

**문서로만 남을 것:**
- `5a4e81e` 의 실질 산출물은 **"xim = 얼어붙음, 고치지 마시오"** 경고다. `main.rs` 주석과 README 데스크톱 절의 그 문단은 **절대 정리·삭제하지 말 것** — 지우면 같은 함정을 다시 밟는다.

**관행으로 남길 것:**
- **커밋 메시지의 원인 서술을 믿지 말 것.** 8건 중 3건(`63e46eb` 원인, `85c20e2` 전제, `9401ca5` 의 "createHashRouter 라 그대로 돈다"·"src/ 는 한 줄도 안 바뀌었다")이 실측에서 무너졌다. `29936be` 이후 데스크톱은 `createBrowserRouter` 로 돈다 — Tauri 2 는 `file:` 로 서빙하지 않는다.
- **테스트를 목으로 감싸면 배송 경로가 사각지대가 된다.** `29936be` 는 16/16 초록인데 배포본에서 브라우저가 안 열린다. `src-tauri/src` 에는 Rust 테스트가 **0개**라 루프백 바인딩 주소·8KB 상한·0600 권한이 회귀해도 초록이다.
- **인라인 style 단언은 초록이 지킨다는 뜻이 아닌 전형이다.** `63e46eb` 의 단언 4개는 크로미움 기준 화면에 아무 차이도 없는 변경에 빨간불을 켜고, 진짜 버그(코트 밖 배치)는 우연히만 지킨다.
